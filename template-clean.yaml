AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: >
  Clean Engagements Platform with consistent resource naming
  Environment: {Environment}
  Domain: {DomainName}

Parameters:
  Environment:
    Type: String
    AllowedValues: [dev, test, prod]
    Description: Environment name (dev, test, prod)

  StackName:
    Type: String
    Description: Stack name for resource naming (e.g., engagedev, engagetest, engageprod)

  DomainName:
    Type: String
    Description: Primary domain name for this environment
    Default: ""

  SecondaryDomainName:
    Type: String
    Description: Secondary domain name (for prod dual domains)
    Default: ""

  HostedZoneId:
    Type: String
    Description: Route53 hosted zone ID
    Default: "ZB9TUA073B5SH"

  GitHubToken:
    Type: String
    Description: GitHub Personal Access Token for creating issues
    NoEcho: true
    Default: ""

  GitHubRepo:
    Type: String
    Description: GitHub repository in format owner/repo-name
    Default: "geseib/engagements"

  # Google OAuth credentials are passed as values (not SSM references)
  # because CloudFormation does not support ssm-secure dynamic references
  # in Cognito UserPoolIdentityProvider properties. The deploy script
  # (scripts/deploy-clean.sh) fetches them from SSM (/engdev/google/*) and
  # passes them through; when empty, Google OAuth resources are skipped.
  GoogleClientId:
    Type: String
    Description: Google OAuth Client ID (empty disables Google sign-in resources)
    Default: ""

  GoogleClientSecret:
    Type: String
    NoEcho: true
    Description: Google OAuth Client Secret (empty disables Google sign-in resources)
    Default: ""

Conditions:
  HasDomain: !Not [!Equals [!Ref DomainName, ""]]
  HasSecondaryDomain: !Not [!Equals [!Ref SecondaryDomainName, ""]]
  IsProd: !Equals [!Ref Environment, "prod"]
  # True when the deploy passes Google credentials, fetched from
  # /<stack>/google/* SSM parameters by all three buildspecs.
  #
  # ⚠️ WHEN THIS IS FALSE, GOOGLE SIGN-IN IS REMOVED. Not "left alone" —
  # REMOVED. :2520 writes `SupportedIdentityProviders: [COGNITO]` on every
  # deploy, so a provider configured by hand in the Cognito console survives
  # only until the next deploy of that tier.
  #
  # This comment used to say test and prod "stay on console-configured
  # providers", and that sentence was false in a way that cost a production
  # outage on 2026-08-15: buildspec-prod.yml passed a hardcoded override list
  # with no Google parameters, the secret fell to its "" default, and the first
  # prod deploy in 197 commits stripped Google from the app client. Cognito
  # answers such a sign-in with "An error was encountered. Login option is not
  # available. Please try another one."
  #
  # All three buildspecs now fetch the credentials and CONDITIONALLY APPEND
  # them, because passing GoogleClientSecret="" is an explicit override to
  # empty and is not the same as omitting the parameter.
  HasGoogleOAuth: !Not [!Equals [!Ref GoogleClientSecret, ""]]

Globals:
  Function:
    # Lambda runtime for all functions. Bumped nodejs18.x (EOL) -> nodejs22.x on 2026-08-04.
    # Single change point; smoke-test AI + WebSocket functions after any runtime change.
    Runtime: nodejs22.x
    Timeout: 30
    MemorySize: 256
    Environment:
      Variables:
        TABLE_NAME: !Ref GameTable
        WEBSOCKET_API_ENDPOINT: !Sub 'https://${WebSocketApi}.execute-api.${AWS::Region}.amazonaws.com/${Environment}'
        ENVIRONMENT: !Ref Environment
        # Account ID for constructing Bedrock inference-profile ARNs at runtime
        # (replaces hardcoded account IDs that used to live in Lambda source).
        ACCOUNT_ID: !Ref AWS::AccountId
        # The tenant CMK, for lambda-functions/*/tenant-crypto.js. Set globally
        # because the module throws BY NAME when it is missing
        # ("TENANT_KMS_KEY_ID is not set"), and a function that acquires a
        # decrypt path later should fail on its IAM — which is explicit and
        # greppable — rather than on a missing environment variable, which
        # looks like a code bug and is the same symptom as a typo.
        #
        # The variable alone grants nothing. kms:Decrypt is attached per
        # function, deliberately, so that the set of functions able to read
        # customer content stays something you can enumerate from this file.
        TENANT_KMS_KEY_ID: !Ref TenantKey

Resources:

  ### DynamoDB Table with Simple Naming
  GameTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Ref StackName
      BillingMode: PAY_PER_REQUEST
      AttributeDefinitions:
        - AttributeName: PK
          AttributeType: S
        - AttributeName: SK
          AttributeType: S
      KeySchema:
        - AttributeName: PK
          KeyType: HASH
        - AttributeName: SK
          KeyType: RANGE
      # TTL stays ENABLED — GAME#/PLAYER#/CONNECTION# session records rely on it
      # (90 days creation, 7 days active) and disabling it would retain game data
      # forever.
      #
      # It is a per-ITEM opt-in: only records that carry a `ttl` attribute expire.
      # AI prompts and personas (PK=AIPROMPTS) are CONFIGURATION and must never
      # be stamped with one. Every prompt writer used to set now+365d, so prompts
      # authored in Aug 2025 began silently disappearing in Aug 2026. Those
      # stamps are gone from create-ai-prompt.js, update-ai-prompt.js and
      # migrate-ai-prompts.js; update-ai-prompt.js additionally REMOVEs a stale
      # `ttl` on every save. Do not reintroduce one on an AIPROMPT#/PERSONA# item.
      TimeToLiveSpecification:
        AttributeName: ttl
        Enabled: true
      StreamSpecification:
        StreamViewType: NEW_AND_OLD_IMAGES
      PointInTimeRecoverySpecification:
        PointInTimeRecoveryEnabled: !If [IsProd, true, false]
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: StackName
          Value: !Ref StackName

  ### WebSocket API with Simple Naming
  WebSocketApi:
    Type: AWS::ApiGatewayV2::Api
    Properties:
      Name: !Sub '${StackName}-api-ws'
      ProtocolType: WEBSOCKET
      RouteSelectionExpression: $request.body.action
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ### WebSocket Deployment
  WebSocketDeployment:
    Type: AWS::ApiGatewayV2::Deployment
    DependsOn:
      - ConnectRoute
      - DisconnectRoute
      - MessageRoute
    Properties:
      ApiId: !Ref WebSocketApi

  ### WebSocket Stage
  WebSocketStage:
    Type: AWS::ApiGatewayV2::Stage
    Properties:
      StageName: !Ref Environment
      Description: !Sub '${Environment} stage for WebSocket API'
      DeploymentId: !Ref WebSocketDeployment
      ApiId: !Ref WebSocketApi
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ### Lambda Functions with Simple Naming

  ## WebSocket Functions
  ConnectFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-connect'
      CodeUri: lambda-functions/websocket/
      Handler: connect.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  DisconnectFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-disconnect'
      CodeUri: lambda-functions/websocket/
      Handler: disconnect.handler
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  MessageFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-message'
      CodeUri: lambda-functions/websocket/
      Handler: message.handler
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Game Management Functions
  CreateGameFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-create-game'
      CodeUri: lambda-functions/websocket/
      Handler: create-game.handler
      Events:
        CreateGame:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games
            Method: post
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # A four-digit join code is not a secret — it is on the projector and
            # typed by everyone in the room. Unauthenticated, every participant, and
            # anyone they forward the code to, could drive somebody else's live
            # session: advance the round out from under the host, push the room into
            # voting before people had answered, or change which categories get
            # asked. POST /games is the other kind of problem — an unauthenticated
            # creator leaves a session with no owner to attribute and no org to bill.
            #
            # THE CLIENT HALF SHIPPED IN THE SAME CHANGE AND HAD TO. Every caller is
            # a signed-in host surface, but they all used plain `fetch`, which sends
            # no Authorization header. Attaching this first would have 401'd the
            # host's own advance button and frozen the room mid-round. GameHostPage,
            # HostRemote and QuickstartMenu moved to authFetch together — including
            # HostRemote's two ${apiBase()}${request.path} dispatches, whose paths
            # are built in config/hostRemote.js and so name no route here.
            #
            # The GROUP check is NOT here. requiredGroupsForRoute answers
            # ['hosts','admins'] via its POST/PUT/DELETE + games rule, which sits
            # ABOVE the generic public rule — so start-vote is not handed to
            # path.includes('vote'). tests/session-control-routes-authorization.js
            # pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  StartQuestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-start-question'
      CodeUri: lambda-functions/websocket/
      Handler: start-question.handler
      Events:
        StartQuestion:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/start-question
            Method: post
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # A four-digit join code is not a secret — it is on the projector and
            # typed by everyone in the room. Unauthenticated, every participant, and
            # anyone they forward the code to, could drive somebody else's live
            # session: advance the round out from under the host, push the room into
            # voting before people had answered, or change which categories get
            # asked. POST /games is the other kind of problem — an unauthenticated
            # creator leaves a session with no owner to attribute and no org to bill.
            #
            # THE CLIENT HALF SHIPPED IN THE SAME CHANGE AND HAD TO. Every caller is
            # a signed-in host surface, but they all used plain `fetch`, which sends
            # no Authorization header. Attaching this first would have 401'd the
            # host's own advance button and frozen the room mid-round. GameHostPage,
            # HostRemote and QuickstartMenu moved to authFetch together — including
            # HostRemote's two ${apiBase()}${request.path} dispatches, whose paths
            # are built in config/hostRemote.js and so name no route here.
            #
            # The GROUP check is NOT here. requiredGroupsForRoute answers
            # ['hosts','admins'] via its POST/PUT/DELETE + games rule, which sits
            # ABOVE the generic public rule — so start-vote is not handed to
            # path.includes('vote'). tests/session-control-routes-authorization.js
            # pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  StartVoteFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-start-vote'
      CodeUri: lambda-functions/websocket/
      Handler: start-vote.handler
      Events:
        StartVote:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/start-vote
            Method: post
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # A four-digit join code is not a secret — it is on the projector and
            # typed by everyone in the room. Unauthenticated, every participant, and
            # anyone they forward the code to, could drive somebody else's live
            # session: advance the round out from under the host, push the room into
            # voting before people had answered, or change which categories get
            # asked. POST /games is the other kind of problem — an unauthenticated
            # creator leaves a session with no owner to attribute and no org to bill.
            #
            # THE CLIENT HALF SHIPPED IN THE SAME CHANGE AND HAD TO. Every caller is
            # a signed-in host surface, but they all used plain `fetch`, which sends
            # no Authorization header. Attaching this first would have 401'd the
            # host's own advance button and frozen the room mid-round. GameHostPage,
            # HostRemote and QuickstartMenu moved to authFetch together — including
            # HostRemote's two ${apiBase()}${request.path} dispatches, whose paths
            # are built in config/hostRemote.js and so name no route here.
            #
            # The GROUP check is NOT here. requiredGroupsForRoute answers
            # ['hosts','admins'] via its POST/PUT/DELETE + games rule, which sits
            # ABOVE the generic public rule — so start-vote is not handed to
            # path.includes('vote'). tests/session-control-routes-authorization.js
            # pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  RevealAuthorsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-reveal-authors'
      CodeUri: lambda-functions/game/
      Handler: reveal-authors.handler
      Events:
        RevealAuthors:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/reveal-authors
            Method: post
            # HOST ONLY. This ends the anonymity of a round for the whole room
            # and answers with every author's name. A participant knows the
            # four-digit game id, so without this any player could have flipped
            # it from a phone. Players never call it — the reveal they see
            # arrives over the WebSocket.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  StageBeatFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-stage-beat'
      CodeUri: lambda-functions/game/
      Handler: stage-beat.handler
      Runtime: nodejs22.x
      Timeout: 30
      Events:
        StageBeatEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/stage-beat
            Method: post
            # HOST ONLY, exactly like /close-round and /reveal-authors above.
            # This moves what the whole room is looking at — from the round's
            # tally to the AI's read-back and back — and a participant knows the
            # four-digit game id, so on a public route it would be a button any
            # phone in the room could press mid-discussion.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        # The beat is only useful if the OTHER surface hears it: the projector
        # when the host taps the phone, the phone when the host taps the
        # projector. Without this the broadcast is a silent AccessDenied and the
        # two devices disagree about what the room is reading.
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  UpNextFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-up-next'
      CodeUri: lambda-functions/game/
      Handler: up-next.handler
      Runtime: nodejs22.x
      Timeout: 30
      Events:
        UpNextEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/up-next
            Method: get
            # HOST ONLY, and this one is not a judgement call. The response
            # ENUMERATES QUESTIONS THAT HAVE NOT BEEN ASKED YET. On a trivia
            # round, knowing the next three questions is most of the way to
            # knowing the next three answers, and a participant already holds
            # the four-digit game id.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        # Read-only by design — see the handler's header. Peeking at the running
        # order must not spend a question, or checking what is coming would
        # change what is coming.
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  StageFocusFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-stage-focus'
      CodeUri: lambda-functions/game/
      Handler: stage-focus.handler
      Runtime: nodejs22.x
      Timeout: 30
      Events:
        StageFocusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/stage-focus
            Method: post
            # HOST ONLY, exactly like /stage-beat above and for a sharper
            # version of its reason. This does not merely move the room between
            # two beats of one round — it puts ONE NAMED PERSON'S RESPONSE
            # full-screen on a wall. On a public route that is a button any
            # phone holding the four-digit game id could press, about somebody
            # else's answer.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        # Crud, NOT Write: the 410-Gone reaping in this handler's broadcast
        # deletes dead CONNECTION# rows, and SAM's DynamoDBWritePolicy grants
        # Put/Update/BatchWrite but NOT DeleteItem. With Write only, every stale
        # connection survives forever and is retried on every future broadcast —
        # and the failure is an AccessDenied inside a catch that never throws,
        # so nothing surfaces anywhere.
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  QuestionQueueFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-question-queue'
      CodeUri: lambda-functions/game/
      Handler: question-queue.handler
      Runtime: nodejs22.x
      Timeout: 30
      Events:
        # Two events, one function, because they are two halves of one fact:
        # the panel reads the running order and then edits it, and a split
        # across two handlers would be two copies of the projection shape.
        ReadQueue:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/queue
            Method: get
            # HOST ONLY, exactly like /stage-beat and /reveal-authors above.
            # The queue names the questions the room has NOT been asked yet, so
            # on a public route it is a spoiler served to any phone holding the
            # four-digit game id — and players have that id by definition.
            Auth:
              Authorizer: CognitoAuthorizer
        ChangeQueue:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/queue
            Method: post
            # HOST ONLY, and here it decides what the whole room is asked next.
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        # CRUD, NOT Read+Write, and the difference is not cosmetic: SAM's
        # DynamoDBWritePolicy grants PutItem/UpdateItem/BatchWrite but NOT
        # DeleteItem, and this handler deletes — the 410-Gone connection reaping
        # in its broadcast drops dead CONNECTION# rows inline, the same cleanup
        # stage-beat.js and next-question.js do. With Write only, every stale
        # connection survives forever and is retried on every future broadcast,
        # and the failure is an AccessDenied inside a catch that deliberately
        # never throws, so nothing surfaces anywhere.
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        # The OTHER host surface has to hear about the change. Queue on the
        # phone, watch the stage follow; reorder on the stage, watch the phone
        # follow. Without this the broadcast is a silent AccessDenied and the
        # two devices disagree about what is queued.
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  QuestionExclusionsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-question-exclusions'
      CodeUri: lambda-functions/game/
      Handler: question-exclusions.handler
      Runtime: nodejs22.x
      Timeout: 30
      Events:
        # The queue's sibling: the questions the host has DISABLED for this
        # session ("move up/move down/disable/move back out" — the third verb).
        # Same one-function read+write shape as /queue, same reason.
        ReadExclusions:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/exclusions
            Method: get
            # HOST ONLY, exactly like /queue: the list names unasked questions,
            # which is a spoiler on any public route.
            Auth:
              Authorizer: CognitoAuthorizer
        ChangeExclusions:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/exclusions
            Method: post
            Auth:
              Authorizer: CognitoAuthorizer
      Policies:
        # Read the row, write the row. No DeleteItem, no broadcast: unlike the
        # queue this endpoint reaps no connections and tells no other surface —
        # the panel that changed it reloads itself, and the drain reads the row
        # fresh every round.
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ### HTTP API with Simple Naming (like original GameApi)
  RestApi:
    Type: AWS::Serverless::HttpApi
    Properties:
      StageName: !Ref Environment
      CorsConfiguration:
        AllowOrigins: ["*"]
        AllowMethods: ["OPTIONS","GET","POST","PUT","DELETE"]
        # X-Engage-Org IS LOAD-BEARING AND ITS ABSENCE IS A TOTAL OUTAGE.
        #
        # `authFetch` attaches it to every authenticated request. A custom
        # header makes the request non-simple, so the browser preflights — and
        # a preflight naming a header that is not on this list is answered 204
        # with NO access-control-allow-* headers at all. The browser then blocks
        # the real request and reports a NETWORK ERROR to the page.
        #
        # Nothing outside a browser can see it: the API is healthy, the logs are
        # empty because the request never arrives, and every curl smoke check
        # passes. It is also intermittent in the worst way — until an
        # organisation has been stored, authFetch sends no org header and
        # everything works, so it presents as "some screens are broken".
        #
        # tests/cors-allows-sent-headers.js reads this list and the headers
        # authFetch actually sends, and fails if they drift apart.
        AllowHeaders: ["Content-Type", "Authorization", "X-Engage-Org"]
        ExposeHeaders: ["Content-Length", "X-Request-Id"]
      # Lambda authorizer (UserPoolV2 JWTs). No DefaultAuthorizer on purpose:
      # player and host game routes stay public; admin routes opt in per-event
      # with "Auth: Authorizer: CognitoAuthorizer". Exception: the
      # /admin/create-github-issue feedback route stays public because the
      # player page's IssueFab uses it anonymously.
      Auth:
        Authorizers:
          CognitoAuthorizer:
            AuthorizerPayloadFormatVersion: "2.0"
            EnableSimpleResponses: true
            FunctionArn: !GetAtt AuthorizerFunction.Arn
            Identity:
              Headers:
                - Authorization
                # X-Engage-Org IS DELIBERATELY NOT LISTED HERE. Read this before
                # adding it.
                #
                # The active organisation genuinely is part of a request's
                # identity — one token, several orgs, a different role in each —
                # so listing it looks obviously right. It is not, yet, and the
                # failure mode is total.
                #
                # An HTTP API returns 401 WITHOUT INVOKING THE AUTHORIZER when a
                # configured identity source is absent from the request. Every
                # client that has not yet been taught to send the header — which
                # today is all of them — would stop being able to sign in to
                # anything. The authorizer does not need the entry to see the
                # header anyway: it reads event.headers['x-engage-org'] directly,
                # and an identitySource entry only ever affects CACHE KEYING.
                #
                # It becomes mandatory the moment authorizer caching is turned
                # on. With a TTL set and this header missing from the cache key,
                # API Gateway would serve one organisation's authorisation
                # decision to a request for another — a cross-tenant leak
                # introduced by a performance tweak that looks unrelated to
                # tenancy. tests/authorizer-identity-source.js fails the build if
                # AuthorizerResultTtlInSeconds ever appears without it.
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## ── TENANT ENCRYPTION ────────────────────────────────────────────────────
  #
  # ONE customer-managed key for the whole stack, not one per organisation.
  # A per-org CMK is $1/month, which against a $5/month subscription is 20% of
  # revenue before a single session runs. Instead every org gets its own 256-bit
  # DATA key, generated under this CMK with `EncryptionContext: {orgId}`; the
  # wrapped blob lives on ORG#<id>/METADATA and the plaintext is cached in the
  # Lambda container. Field crypto is AES-256-GCM in node:crypto with the orgId
  # as AAD, so there is no KMS call per item — see lambda-functions/*/tenant-crypto.js.
  TenantKey:
    Type: AWS::KMS::Key
    DeletionPolicy: Retain
    UpdateReplacePolicy: Retain
    Properties:
      Description: !Sub 'Engage ${StackName} — per-organisation content encryption'
      EnableKeyRotation: true
      KeyPolicy:
        Version: '2012-10-17'
        Statement:
          # The account root, without which the key cannot be administered and
          # becomes unmanageable. This is the statement AWS requires.
          - Sid: RootAdministration
            Effect: Allow
            Principal: { AWS: !Sub 'arn:aws:iam::${AWS::AccountId}:root' }
            Action: 'kms:*'
            Resource: '*'

          # ── THE STATEMENT THAT DOES THE ACTUAL WORK ──────────────────────
          #
          # There is no decrypt that does not name a tenant. Every Decrypt and
          # GenerateDataKey must supply an `orgId` encryption context, or it is
          # denied — by anyone, including the root principal above, because an
          # explicit Deny wins over any Allow.
          #
          # THIS IS WHAT MAKES THE PRIVACY PAGE TRUE. 08-privacy.html tells a
          # customer "it is not that we cannot — it is that we cannot do it
          # quietly", and this is the mechanism: CloudTrail records
          # `requestParameters.encryptionContext.orgId` on every call, so the
          # decrypt log IS a per-tenant read log. A blanket "let me read the
          # table" is not expressible.
          #
          # It also means S3 SSE-KMS CANNOT use this key: S3 supplies its own
          # object-ARN encryption context, so it would be denied here. The
          # report body is encrypted in the application before the put instead
          # (game/save-report.js) — a DynamoDB-only encryption story would
          # leave the PDF readable and defeat the whole thing.
          - Sid: DenyDecryptWithoutTenantContext
            Effect: Deny
            Principal: '*'
            Action:
              - kms:Decrypt
              - kms:GenerateDataKey
            Resource: '*'
            Condition:
              'Null':
                'kms:EncryptionContext:orgId': 'true'

  TenantKeyAlias:
    Type: AWS::KMS::Alias
    Properties:
      AliasName: !Sub 'alias/${StackName}-tenant'
      TargetKeyId: !Ref TenantKey

  ## ── ORGANISATIONS ────────────────────────────────────────────────────────
  #
  # EVERY ROUTE HERE CARRIES THE AUTHORIZER, INCLUDING THE INVITE ACCEPT.
  # `/orgs/*` and `/invites/*` do not begin with `admin`, so they get no help
  # from the `path.startsWith('admin')` rule; auth/authorizer.js names them
  # explicitly (ORG_ROUTE / INVITE_ROUTE) rather than falling through to the
  # trailing default, because two `path.includes()` rules sit in between and an
  # invite token is 32 base58 characters travelling in the path — one that
  # happened to spell "join", "vote" or "answer" made its own accept route
  # public. tests/org-route-authorization.js pins both halves.
  #
  # WHICH org a caller may act on is decided per row by
  # admin/orgs/shared/org-guards.js, which additionally requires a MEMBER row
  # and the right role. Reaching a route is not being allowed to use it.
  OrgsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-orgs'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/create-org.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          TENANT_KMS_KEY_ID: !Ref TenantKey
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt, kms:GenerateDataKey ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        # TransactWriteItems is NOT covered by DynamoDBCrudPolicy, and org
        # creation, invite acceptance, role changes and member removal are all
        # transactional — a half-created org (index row but no member row, or a
        # member row with no reverse row) is invisible to its own owner.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ dynamodb:TransactWriteItems ]
              Resource: !GetAtt GameTable.Arn
        # GenerateDataKey ONLY — this is the one function that mints an org's
        # data key, at creation. Nothing else in the stack may create one.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:GenerateDataKey ]
              Resource: !GetAtt TenantKey.Arn
      Events:
        CreateOrg:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgsListFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-list-my-orgs'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/list-my-orgs.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt, kms:GenerateDataKey ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        ListMyOrgs:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # ── WHICH ORGANISATIONS ARE WAITING FOR ME ─────────────────────────────
  # Read by the landing screen after sign-in. An invitation is matched to the
  # caller's own Cognito email, which is the check accept-invite.js already
  # made — so no link has to be delivered for somebody who already has an
  # account. See lambda-functions/admin/orgs/list-my-invites.js.
  MyInvitesFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-list-my-invites'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/list-my-invites.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        # No kms:Decrypt. An invitation carries an email address, a role and the
        # inviting organisation's NAME — all plaintext by design, because the
        # invitee is not a member of that organisation and could not decrypt
        # anything of its content even if this handed them some.
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        ListMyInvites:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /invites
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # ── THE PLATFORM CONSOLE ───────────────────────────────────────────────
  # Engage staff only (tenant.isPlatformAdmin, never an org role). It lists
  # organisations and changes one's status; there is deliberately no route from
  # here into any organisation's content, which is the isolation guarantee
  # expressed as an API surface rather than as a promise.
  PlatformOrgsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-platform-orgs'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/platform-orgs.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        # NO kms:Decrypt. This function reads the ORGS index and MEMBER counts,
        # both of which are plaintext by design, and it must not be able to
        # unwrap any organisation's data key — the absence of this statement is
        # part of the guarantee, not an oversight. tests/kms-grants-match-code.js
        # derives the grant set from the require graph and will fail if this
        # function ever pulls tenant-crypto in.
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        ListOrgs:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /platform/orgs
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
        SetOrgStatus:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /platform/orgs/{orgId}/status
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # ── COPY A SHARED SET INTO AN ORGANISATION ─────────────────────────────
  # The missing middle step: every org can READ the platform and public
  # libraries and may not CHANGE them, so without this the only answer to
  # "can I adapt this?" was no. The copy is independent of its source.
  CopyQuestionSetFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-copy-question-set'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 60
      Handler: copy-question-set.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        # DECRYPT ONLY, NEVER GenerateDataKey.
        #
        # It does need the key — the questions are encrypted on the way in, or
        # the copy would sit in the clear inside a partition whose owner
        # believes it is not. But it writes into an organisation that already
        # EXISTS, so it unwraps that org's stored data key and never mints one.
        # Minting is reserved to the two paths that create organisations
        # (create-org.js and personal-org.js), and tests/tenant-infrastructure.js
        # §3 plus tests/kms-grants-match-code.js both fail the moment a third
        # function can mint — which is how this line got narrowed: it shipped
        # with both actions and they caught it.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        CopySet:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /question-sets/{setId}/copy
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgGetFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-get-org'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/get-org.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetOrg:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs/{orgId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgMembersFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-list-members'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/list-members.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        ListMembers:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs/{orgId}/members
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgInviteFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-invite-member'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/invite-member.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        InviteMember:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs/{orgId}/invites
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgRevokeInviteFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-revoke-invite'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/revoke-invite.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        RevokeInvite:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs/{orgId}/invites/{token}
            Method: DELETE
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgAcceptInviteFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-accept-invite'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/accept-invite.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ dynamodb:TransactWriteItems ]
              Resource: !GetAtt GameTable.Arn
      Events:
        AcceptInvite:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /invites/{token}/accept
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgMemberRoleFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-change-member-role'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/change-member-role.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ dynamodb:TransactWriteItems ]
              Resource: !GetAtt GameTable.Arn
      Events:
        ChangeRole:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs/{orgId}/members/{sub}/role
            Method: PUT
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  OrgRemoveMemberFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-org-remove-member'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: orgs/remove-member.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ dynamodb:TransactWriteItems ]
              Resource: !GetAtt GameTable.Arn
      Events:
        RemoveMember:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs/{orgId}/members/{sub}
            Method: DELETE
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## ── METERING ─────────────────────────────────────────────────────────────
  GetUsageFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-usage'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: get-usage.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
      Events:
        GetUsage:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /orgs/{orgId}/usage
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # THE TABLE'S STREAM HAS BEEN ENABLED SINCE DAY ONE WITH NO CONSUMER.
  # This is its first. Stored-set billing is a PEAK over the period, so when a
  # stream record touches an ORG#…#SETS partition the handler RE-QUERIES that
  # partition's count and conditionally raises the peak. Recompute rather than
  # increment: streams deliver at-least-once, an increment double-counts on
  # redelivery, and a recompute cannot.
  UsageStreamFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-usage-stream'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 60
      Handler: usage-stream.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - DynamoDBStreamReadPolicy:
            TableName: !Ref GameTable
            StreamName: !Select [3, !Split ['/', !GetAtt GameTable.StreamArn]]
      Events:
        TableStream:
          Type: DynamoDB
          Properties:
            Stream: !GetAtt GameTable.StreamArn
            StartingPosition: LATEST
            BatchSize: 100
            MaximumBatchingWindowInSeconds: 10
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # The backstop for a shard the stream consumer missed. It repairs counters
  # from the LEDGER, which is the authority — the ledger row is written before
  # the counter, so "no ledger rows but a non-zero counter" is unreachable from
  # our own code and is left alone rather than zeroed.
  UsageReconcileFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-usage-reconcile'
      CodeUri: lambda-functions/admin/
      Runtime: nodejs22.x
      Timeout: 300
      Handler: usage-reconcile.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        Nightly:
          Type: Schedule
          Properties:
            Schedule: cron(5 0 * * ? *)
            Description: Rebuild org usage counters from the ledger
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # The reader for an encrypted saved report. See lambda-functions/game/
  # download-report.js for why the object is an envelope and why SSE-KMS under
  # the tenant key is not an option.
  #
  # PUBLIC, deliberately and unchanged: it replaces a presigned S3 link, which
  # was a bearer URL too. The UI's "Copy Link" is a report sent to a colleague;
  # authenticating it would break that, which is the feature.
  DownloadReportFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-download-report'
      CodeUri: lambda-functions/game/
      Runtime: nodejs22.x
      Timeout: 30
      Handler: download-report.handler
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          REPORTS_BUCKET_NAME: !Ref ReportsBucket
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
        - S3ReadPolicy:
            BucketName: !Ref ReportsBucket
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
      Events:
        DownloadReport:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/report/download
            Method: GET
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Admin Functions
  AdminGetQuestionSetsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-get-question-sets'
      CodeUri: lambda-functions/admin/
      Handler: get-question-sets.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminGetQuestionSetsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/question-sets
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminGetQuestionSetQuestionsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-get-question-set-questions'
      CodeUri: lambda-functions/admin/
      Handler: get-question-set-questions.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminGetQuestionSetQuestionsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /question-sets/{setId}/questions
            Method: GET
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # Question sets are the product's content: the questions, the answers,
            # the categories, the instructions. Unauthenticated, any caller could
            # read all of it. The participant surface was the proof — PlayerPage
            # fetched the WHOLE catalogue every round to read two strings about its
            # own set, so every anonymous player received the library.
            #
            # CLOSING THIS WAS IMPOSSIBLE UNTIL THE PLAYER STOPPED NEEDING IT.
            # game/get-question.js now projects setCustomInstruction and
            # setRoundNoun onto GET /games/{gameId}/question, from the SETS row it
            # already reads. Attaching this authorizer first would have 401'd every
            # player mid-round.
            #
            # The GROUP check is NOT here. An authorizer proves you are someone, not
            # that you are allowed to be here — requiredGroupsForRoute in
            # lambda-functions/auth/authorizer.js names these routes explicitly, and
            # tests/question-set-routes-authorization.js pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Main Question Sets Function (for game creation)
  GetQuestionSetsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-question-sets'
      CodeUri: lambda-functions/game/
      Handler: get-question-sets.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetQuestionSetsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /question-sets
            Method: GET
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # Question sets are the product's content: the questions, the answers,
            # the categories, the instructions. Unauthenticated, any caller could
            # read all of it. The participant surface was the proof — PlayerPage
            # fetched the WHOLE catalogue every round to read two strings about its
            # own set, so every anonymous player received the library.
            #
            # CLOSING THIS WAS IMPOSSIBLE UNTIL THE PLAYER STOPPED NEEDING IT.
            # game/get-question.js now projects setCustomInstruction and
            # setRoundNoun onto GET /games/{gameId}/question, from the SETS row it
            # already reads. Attaching this authorizer first would have 401'd every
            # player mid-round.
            #
            # The GROUP check is NOT here. An authorizer proves you are someone, not
            # that you are allowed to be here — requiredGroupsForRoute in
            # lambda-functions/auth/authorizer.js names these routes explicitly, and
            # tests/question-set-routes-authorization.js pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Game Flow Functions
  GetGameFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-game'
      CodeUri: lambda-functions/game/
      Handler: get-game.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetGameEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}
            Method: GET
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  StartGameFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-start-game'
      CodeUri: lambda-functions/game/
      Handler: start-game.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        StartGameEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/start
            Method: POST
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # A four-digit join code is not a secret — it is on the projector and
            # typed by everyone in the room. Unauthenticated, every participant, and
            # anyone they forward the code to, could drive somebody else's live
            # session: advance the round out from under the host, push the room into
            # voting before people had answered, or change which categories get
            # asked. POST /games is the other kind of problem — an unauthenticated
            # creator leaves a session with no owner to attribute and no org to bill.
            #
            # THE CLIENT HALF SHIPPED IN THE SAME CHANGE AND HAD TO. Every caller is
            # a signed-in host surface, but they all used plain `fetch`, which sends
            # no Authorization header. Attaching this first would have 401'd the
            # host's own advance button and frozen the room mid-round. GameHostPage,
            # HostRemote and QuickstartMenu moved to authFetch together — including
            # HostRemote's two ${apiBase()}${request.path} dispatches, whose paths
            # are built in config/hostRemote.js and so name no route here.
            #
            # The GROUP check is NOT here. requiredGroupsForRoute answers
            # ['hosts','admins'] via its POST/PUT/DELETE + games rule, which sits
            # ABOVE the generic public rule — so start-vote is not handed to
            # path.includes('vote'). tests/session-control-routes-authorization.js
            # pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # Edit a session BEFORE it starts — narrow UpdateCommands on the whitelisted
  # METADATA attributes, mirrored onto the GAMES index row for Title/Visibility.
  # Refuses any session whose STATE is not CREATED (start-game's predicate).
  # Carries the Cognito authorizer: authorizer.js requires hosts/admins for a
  # PUT on a games path, and this route must not repeat CreateGameFunction's
  # missing-Auth omission.
  UpdateGameFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-update-game'
      CodeUri: lambda-functions/game/
      Handler: update-game.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        UpdateGameEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}
            Method: PUT
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  NextQuestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-next-question'
      CodeUri: lambda-functions/game/
      Handler: next-question.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Events:
        NextQuestionEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/next-question
            Method: POST
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # A four-digit join code is not a secret — it is on the projector and
            # typed by everyone in the room. Unauthenticated, every participant, and
            # anyone they forward the code to, could drive somebody else's live
            # session: advance the round out from under the host, push the room into
            # voting before people had answered, or change which categories get
            # asked. POST /games is the other kind of problem — an unauthenticated
            # creator leaves a session with no owner to attribute and no org to bill.
            #
            # THE CLIENT HALF SHIPPED IN THE SAME CHANGE AND HAD TO. Every caller is
            # a signed-in host surface, but they all used plain `fetch`, which sends
            # no Authorization header. Attaching this first would have 401'd the
            # host's own advance button and frozen the room mid-round. GameHostPage,
            # HostRemote and QuickstartMenu moved to authFetch together — including
            # HostRemote's two ${apiBase()}${request.path} dispatches, whose paths
            # are built in config/hostRemote.js and so name no route here.
            #
            # The GROUP check is NOT here. requiredGroupsForRoute answers
            # ['hosts','admins'] via its POST/PUT/DELETE + games rule, which sits
            # ABOVE the generic public rule — so start-vote is not handed to
            # path.includes('vote'). tests/session-control-routes-authorization.js
            # pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ToggleCategoryFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-toggle-category'
      CodeUri: lambda-functions/game/
      Handler: toggle-category.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        ToggleCategoryEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/toggle-category
            Method: POST
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # A four-digit join code is not a secret — it is on the projector and
            # typed by everyone in the room. Unauthenticated, every participant, and
            # anyone they forward the code to, could drive somebody else's live
            # session: advance the round out from under the host, push the room into
            # voting before people had answered, or change which categories get
            # asked. POST /games is the other kind of problem — an unauthenticated
            # creator leaves a session with no owner to attribute and no org to bill.
            #
            # THE CLIENT HALF SHIPPED IN THE SAME CHANGE AND HAD TO. Every caller is
            # a signed-in host surface, but they all used plain `fetch`, which sends
            # no Authorization header. Attaching this first would have 401'd the
            # host's own advance button and frozen the room mid-round. GameHostPage,
            # HostRemote and QuickstartMenu moved to authFetch together — including
            # HostRemote's two ${apiBase()}${request.path} dispatches, whose paths
            # are built in config/hostRemote.js and so name no route here.
            #
            # The GROUP check is NOT here. requiredGroupsForRoute answers
            # ['hosts','admins'] via its POST/PUT/DELETE + games rule, which sits
            # ABOVE the generic public rule — so start-vote is not handed to
            # path.includes('vote'). tests/session-control-routes-authorization.js
            # pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # Mid-game voice switch. Deliberately a narrow UpdateCommand on one attribute
  # — see the header of update-game-persona.js for why reviving
  # websocket/save-game-context.js would have destroyed the METADATA item.
  UpdateGamePersonaFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-update-game-persona'
      CodeUri: lambda-functions/game/
      Handler: update-game-persona.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        UpdateGamePersonaEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/persona
            Method: PUT
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # A four-digit join code is not a secret — it is on the projector and
            # typed by everyone in the room. Unauthenticated, every participant, and
            # anyone they forward the code to, could drive somebody else's live
            # session: advance the round out from under the host, push the room into
            # voting before people had answered, or change which categories get
            # asked. POST /games is the other kind of problem — an unauthenticated
            # creator leaves a session with no owner to attribute and no org to bill.
            #
            # THE CLIENT HALF SHIPPED IN THE SAME CHANGE AND HAD TO. Every caller is
            # a signed-in host surface, but they all used plain `fetch`, which sends
            # no Authorization header. Attaching this first would have 401'd the
            # host's own advance button and frozen the room mid-round. GameHostPage,
            # HostRemote and QuickstartMenu moved to authFetch together — including
            # HostRemote's two ${apiBase()}${request.path} dispatches, whose paths
            # are built in config/hostRemote.js and so name no route here.
            #
            # The GROUP check is NOT here. requiredGroupsForRoute answers
            # ['hosts','admins'] via its POST/PUT/DELETE + games rule, which sits
            # ABOVE the generic public rule — so start-vote is not handed to
            # path.includes('vote'). tests/session-control-routes-authorization.js
            # pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  GetQuestionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-question'
      CodeUri: lambda-functions/game/
      Handler: get-question.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetQuestionEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/question
            Method: GET
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName


  GetAnswersFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-answers'
      CodeUri: lambda-functions/game/
      Handler: get-answers.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetAnswersEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/answers
            Method: GET
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName


  GetResultsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-results'
      CodeUri: lambda-functions/game/
      Handler: get-results.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        # get-results moves the game into RESULTS# and now announces it over the
        # WebSocket, the way next-question and start-vote already announce their
        # transitions. Without this the announcement is a silent AccessDenied and
        # the room stays on the voting screen while the database says RESULTS.
        # (WEBSOCKET_API_ENDPOINT comes from Globals, so only IAM was missing.)
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
        # Wavelength clustering: closing a wavelength round fires an
        # InvocationType:'Event' self-invoke (the get-ai-summary worker pattern)
        # whose worker asks Bedrock which words are the same term (plurals,
        # misspellings, abbreviations) before re-running the unanimity analysis.
        # Without these the dispatch fails and every round stands on exact
        # matching — announced on screen, but never the clustered result the
        # spec (docs/superpowers/specs/2026-08-09-wavelength-convergence-design.md)
        # calls for.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-get-results'
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
      Events:
        # THE READ. Public, and it has to be: PlayerPage calls this with a
        # plain fetch the moment the room enters RESULTS, so an authorizer
        # here would break every player client. The handler refuses to perform
        # the transition on this route — it will only report a round that is
        # already resolved (get-results.js: isHostTransitionRoute).
        GetResultsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/get-results
            Method: POST
        # THE TRANSITION. Host only.
        #
        # Same handler, second route — HTTP API authorizers are per-route and
        # not optional, so "public to read, authenticated to write" cannot be
        # expressed on one route. Closing a round writes RESULTS#nnn, flips
        # AuthorsRevealed (ending the room's anonymity), awards scores and
        # announces the move to everyone. A participant knows the four-digit
        # game id, so on a public route that was a button any phone in the room
        # could press mid-vote. Same reasoning as /reveal-authors above.
        CloseRoundEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/close-round
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  CreateReportFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-create-report'
      CodeUri: lambda-functions/game/
      Handler: create-report.handler
      Events:
        CreateReportEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/report
            Method: POST
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  GetReportFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-report'
      CodeUri: lambda-functions/game/
      Handler: get-report.handler
      Events:
        GetReportEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/report
            Method: GET
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  SaveReportFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-save-report'
      CodeUri: lambda-functions/game/
      Handler: save-report.handler
      Timeout: 60
      Events:
        SaveReportEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/save-report
            Method: POST
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3CrudPolicy:
            BucketName: !Ref ReportsBucket
      Environment:
        Variables:
          REPORTS_BUCKET_NAME: !Ref ReportsBucket
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  GetAISummaryFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-ai-summary'
      CodeUri: lambda-functions/game/
      Handler: get-ai-summary.handler
      Runtime: nodejs22.x
      Timeout: 300
      MemorySize: 512
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3ReadPolicy:
            BucketName: !Ref AIPromptsBucket
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
        # Async generation: self-invoke worker (Event) + WebSocket broadcast of aiSummaryReady/aiSummaryError
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-get-ai-summary'
            - Effect: Allow
              Action: execute-api:ManageConnections
              Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Events:
        GetAISummaryEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/ai-summary
            Method: GET
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName


  GetPlayersFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-players'
      CodeUri: lambda-functions/game/
      Handler: get-players.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetPlayersEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/players
            Method: GET

  GetGamesListFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-games-list'
      CodeUri: lambda-functions/game/
      Handler: get-games-list.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetGamesListEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games
            Method: GET
            # HOST/ADMIN ONLY, and it is the odd one out among the GET /games/*
            # routes around it, so the reason is written here.
            #
            # Everything else a participant's client calls names ONE game — the
            # one whose four-digit code they were given. This route names them
            # ALL: every title, every host's name and every join code in the
            # environment, in one request. Nothing on the player path calls it
            # (PlayerPage.jsx and RootPage.jsx make no such request); the only
            # callers are the host's switch-game/reports dialog
            # (GameHostPage.jsx fetchGamesList) and the admin Sessions tab
            # (SessionsPanel.jsx), and both have a signed-in user in hand.
            #
            # Public, it also cancelled the one protection four-digit codes had.
            # Enumerability was accepted on the grounds that an attacker must
            # guess 10,000 codes noisily; this handed over the answer key, with
            # `started` and `visibility` to pick the live, code-free ones.
            #
            # The GROUP check is NOT here. An HTTP API authorizer proves you are
            # someone, not that you are allowed to be here — attaching this
            # alone would still admit any pool account, including one still in
            # `pending`. See requiredGroupsForRoute in
            # lambda-functions/auth/authorizer.js, which must name this route
            # explicitly. The two halves only work shipped together, and
            # tests/games-list-authorization.js pins both.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  GetGameStateFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-game-state'
      CodeUri: lambda-functions/game/
      Handler: get-game-state.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetGameStateEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/state
            Method: GET
        GetGameStateWithPlayerEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/state/{playerId}
            Method: GET
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  GetCategoriesFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-categories'
      CodeUri: lambda-functions/game/
      Handler: get-categories.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetCategoriesEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /question-sets/{setId}/categories
            Method: GET
            # HOST/ADMIN ONLY. This route was PUBLIC.
            #
            # Question sets are the product's content: the questions, the answers,
            # the categories, the instructions. Unauthenticated, any caller could
            # read all of it. The participant surface was the proof — PlayerPage
            # fetched the WHOLE catalogue every round to read two strings about its
            # own set, so every anonymous player received the library.
            #
            # CLOSING THIS WAS IMPOSSIBLE UNTIL THE PLAYER STOPPED NEEDING IT.
            # game/get-question.js now projects setCustomInstruction and
            # setRoundNoun onto GET /games/{gameId}/question, from the SETS row it
            # already reads. Attaching this authorizer first would have 401'd every
            # player mid-round.
            #
            # The GROUP check is NOT here. An authorizer proves you are someone, not
            # that you are allowed to be here — requiredGroupsForRoute in
            # lambda-functions/auth/authorizer.js names these routes explicitly, and
            # tests/question-set-routes-authorization.js pins both halves.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  JoinGameFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-join-game'
      CodeUri: lambda-functions/game/
      Handler: join-game.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Events:
        JoinGameEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/players
            Method: POST
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # ── THE NAME HANDOVER: ONE ASKS, THE OTHER GRANTS ────────────────────────
  #
  # A player refused with NAME_TAKEN can ask the host to hand the name over;
  # the host grants it for exactly one exchange. The two routes are one path
  # segment apart and have OPPOSITE auth, which is the design and not an
  # oversight — see the header of each handler.

  RequestHandoverFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-request-handover'
      CodeUri: lambda-functions/game/
      Handler: request-handover.handler
      Runtime: nodejs22.x
      Timeout: 30
      # DERIVED FROM THE CALLS THE HANDLER MAKES, not copied from its neighbour.
      #   session-gate.js  GetCommand      -> Read
      #   the ask itself   UpdateCommand   -> Write
      #   host-notify.js   QueryCommand    -> Read
      #                    DeleteCommand   -> ONLY in Crud; Read+Write does NOT
      #                    grant DeleteItem (commit 5ac19a6b), and the 410-Gone
      #                    cleanup of a dead host connection needs it.
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Events:
        RequestHandoverEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/players/{playerName}/handover-request
            Method: POST
            # DELIBERATELY NO AUTHORIZER, and it is the one route in this pair
            # that must not have one. The caller is by definition somebody who
            # is NOT in the session — they were just refused a name — so they
            # hold no token and own no row. Gating this would mean the only
            # people who can ask for a handover are hosts and admins, i.e.
            # nobody who ever needs to.
            #
            # It grants nothing: it records "somebody asked" and pings the host.
            # It clears the same gates a join clears first (started; access code
            # for a private session), and answers identically whether or not the
            # name exists, so it is not a roster probe.
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  GrantHandoverFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-grant-handover'
      CodeUri: lambda-functions/game/
      Handler: grant-handover.handler
      Runtime: nodejs22.x
      Timeout: 30
      # GetCommand + UpdateCommand. No websocket send: the person waiting for
      # this has no socket yet (a player's socket opens only after a join
      # succeeds), so there is nobody to push to and the client asks again.
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
        - DynamoDBWritePolicy:
            TableName: !Ref GameTable
      Events:
        GrantHandoverEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/players/{playerName}/handover
            Method: POST
            # HOST ONLY, exactly like /reveal-authors and /stage-beat above and
            # for their reason: every participant knows the four-digit game id,
            # so on a public route this would be a button any phone in the room
            # could press to take another person's answers and score.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  RemovePlayerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-remove-player'
      CodeUri: lambda-functions/game/
      Handler: remove-player.handler
      Runtime: nodejs22.x
      Timeout: 30
      # A SOFT removal: one Get, one Update that sets or removes a single
      # `RemovedAt` attribute. The handler contains no DeleteCommand at all and
      # must never grow one — nothing about a removal may erase an answer, a
      # vote or a score row. `host-notify.js` needs Query, and DeleteItem for
      # the 410-Gone connection cleanup, which is why this is Crud rather than
      # Read+Write: SAM's DynamoDBWritePolicy does NOT include DeleteItem
      # (commit 5ac19a6b).
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Events:
        RemovePlayerEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/players/{playerName}/remove
            Method: POST
            # HOST ONLY. Same reason as the grant above.
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  SubmitVoteFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-submit-vote'
      CodeUri: lambda-functions/game/
      Handler: submit-vote.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          WEBSOCKET_API_ENDPOINT: !Sub 'https://${WebSocketApi}.execute-api.${AWS::Region}.amazonaws.com/${Environment}'
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Statement:
          - Effect: Allow
            Action:
              - execute-api:ManageConnections
            Resource: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*/*'
      Events:
        SubmitVoteEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/votes
            Method: POST
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  GetVotesFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-get-votes'
      CodeUri: lambda-functions/game/
      Handler: get-votes.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        GetVotesEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /games/{gameId}/votes
            Method: GET
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminAIGenerateQuestionsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-generate-questions'
      CodeUri: lambda-functions/admin/
      Handler: ai-generate-questions.handler
      Runtime: nodejs22.x
      Timeout: 900
      MemorySize: 1024
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
        # Async generation: self-invoke worker (Event). Generation cannot run
        # inside the request — API Gateway's 30s integration timeout is a hard
        # ceiling. POST returns 202 and this invoke does the work against the
        # 900s function timeout.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-admin-ai-generate-questions'
      Events:
        AdminAIGenerateQuestionsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-questions
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        # Job status poll. The admin builders have no gameId and no WebSocket,
        # so results come back by polling rather than by broadcast.
        AdminAIGenerateQuestionsStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-questions/{jobId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # Drafts a question SET's own metadata — name, description, customInstruction,
  # aiContextInstruction — from the set's own questions. Those four fields are
  # what QuestionsPanel.buildAiContext() sends to ai-generate-questions above as
  # context.title / description / customInstructions / aiContextInstructions, so
  # a thin description silently degrades every question drafted afterwards. This
  # endpoint repairs the INPUT to the generator declared above it.
  #
  # NOT in auth/authorizer.js's HOST_ADMIN_ROUTES, deliberately: everything under
  # /admin that is not on that list falls through to admins-only, and the AI
  # routes are excluded there because they spend Bedrock budget. The Authorizer
  # below is what enforces it; the handler checks again with require-admin.js.
  AdminAIDraftSetMetadataFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-draft-set-metadata'
      CodeUri: lambda-functions/admin/
      Handler: ai-draft-set-metadata.handler
      Runtime: nodejs22.x
      Timeout: 900
      MemorySize: 1024
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
        # Async generation: self-invoke worker (Event). Four prose fields is a
        # ~8s happy path, but a cold start plus one throttle retry plus the Haiku
        # fallback is three generations and two sleeps — comfortably past API
        # Gateway's hard 30s integration ceiling. POST returns 202 and this
        # invoke does the work against the 900s function timeout.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-admin-ai-draft-set-metadata'
      Events:
        AdminAIDraftSetMetadataEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-draft-set-metadata
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        # Job status poll. Same shape as ai-generate-questions above: the admin
        # builders have no gameId and no WebSocket, so results come back by
        # polling rather than by broadcast.
        AdminAIDraftSetMetadataStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-draft-set-metadata/{jobId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # THE AI HELPER THAT FILLS IN A BUILDER FORM.
  #
  # The owner: "i do wish there was an AI helper that filled out the forms for
  # the user based on some prelim info they offered ... or if the user filled
  # those in the ai would refine (unless locked, a small icon lock/unlock on
  # cells."
  #
  # Same guard as the set-metadata drafter above and for the same reason: NOT in
  # auth/authorizer.js's HOST_ADMIN_ROUTES, so it falls through to admins-only,
  # because reaching it is a Bedrock spend. The handler checks again with
  # require-admin.js. Unlike that one it reads no SETS row — nothing exists yet
  # when this runs, which is the whole premise of the screen — so its DynamoDB
  # access is the job record and nothing else.
  AdminAIDraftBuilderFormFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-draft-builder-form'
      CodeUri: lambda-functions/admin/
      Handler: ai-draft-builder-form.handler
      Runtime: nodejs22.x
      Timeout: 900
      MemorySize: 1024
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
        # Async generation: self-invoke worker (Event). Up to five prose fields
        # is a ~15s happy path, but a cold start plus one throttle retry plus the
        # Haiku fallback is three generations and two sleeps — past API Gateway's
        # hard 30s integration ceiling. POST returns 202 and this invoke does the
        # work against the 900s function timeout.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-admin-ai-draft-builder-form'
      Events:
        AdminAIDraftBuilderFormEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-draft-builder-form
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        # Job status poll. The admin builders have no gameId and no WebSocket,
        # so results come back by polling rather than by broadcast.
        AdminAIDraftBuilderFormStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-draft-builder-form/{jobId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminAIGenerateScenariosFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-generate-scenarios'
      CodeUri: lambda-functions/admin/
      Handler: ai-generate-scenarios.handler
      Runtime: nodejs22.x
      Timeout: 900
      MemorySize: 1024
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
        # Async generation: self-invoke worker (Event). A single detailed
        # scenario measured 33-40s against API Gateway's hard 30s integration
        # timeout, so generation cannot run inside the request. POST returns 202
        # and this invoke does the work against the 900s function timeout.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-admin-ai-generate-scenarios'
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Events:
        AdminAIGenerateScenariosEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-scenarios
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        # Job status poll. The admin builders have no gameId and no WebSocket,
        # so results come back by polling rather than by broadcast.
        AdminAIGenerateScenariosStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-scenarios/{jobId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminAIGenerateTriviaFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-generate-trivia'
      CodeUri: lambda-functions/admin/
      Handler: ai-generate-trivia.handler
      Runtime: nodejs22.x
      Timeout: 900
      MemorySize: 1024
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
        # Async generation: self-invoke worker (Event). Generation cannot run
        # inside the request — API Gateway's 30s integration timeout is a hard
        # ceiling. POST returns 202 and this invoke does the work against the
        # 900s function timeout.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-admin-ai-generate-trivia'
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          REGION: !Ref AWS::Region
          ENVIRONMENT: !Ref Environment
      Events:
        AdminAIGenerateTriviaEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-trivia
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        # Job status poll. The admin builders have no gameId and no WebSocket,
        # so results come back by polling rather than by broadcast.
        AdminAIGenerateTriviaStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-trivia/{jobId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminAIGeneratePollsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-generate-polls'
      CodeUri: lambda-functions/admin/
      Handler: ai-generate-polls.handler
      Runtime: nodejs22.x
      Timeout: 900
      MemorySize: 1024
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
        # Async generation: self-invoke worker (Event). Generation cannot run
        # inside the request — API Gateway's 30s integration timeout is a hard
        # ceiling. POST returns 202 and this invoke does the work against the
        # 900s function timeout.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-admin-ai-generate-polls'
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          REGION: !Ref AWS::Region
          ENVIRONMENT: !Ref Environment
      Events:
        AdminAIGeneratePollsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-polls
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        # Job status poll. The admin builders have no gameId and no WebSocket,
        # so results come back by polling rather than by broadcast.
        AdminAIGeneratePollsStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-polls/{jobId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminAIGenerateSurveyFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-generate-survey'
      CodeUri: lambda-functions/admin/
      Handler: ai-generate-survey.handler
      Runtime: nodejs22.x
      Timeout: 900
      MemorySize: 1024
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
        # Async generation: self-invoke worker (Event). Generation cannot run
        # inside the request — API Gateway's 30s integration timeout is a hard
        # ceiling. POST returns 202 and this invoke does the work against the
        # 900s function timeout.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: lambda:InvokeFunction
              Resource: !Sub 'arn:aws:lambda:${AWS::Region}:${AWS::AccountId}:function:${StackName}-admin-ai-generate-survey'
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          REGION: !Ref AWS::Region
          ENVIRONMENT: !Ref Environment
      Events:
        AdminAIGenerateSurveyEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-survey
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        # Job status poll. The admin builders have no gameId and no WebSocket,
        # so results come back by polling rather than by broadcast.
        AdminAIGenerateSurveyStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-survey/{jobId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminUploadQuestionsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-upload-questions'
      CodeUri: lambda-functions/admin/
      Handler: upload-questions.handler
      Runtime: nodejs22.x
      Timeout: 60
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Events:
        AdminUploadQuestionsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/upload-questions
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminCreateGitHubIssueFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-create-github-issue'
      CodeUri: lambda-functions/admin/
      Handler: create-github-issue.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - secretsmanager:GetSecretValue
              Resource: 
                - !Sub 'arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:${DomainName}-token*'
                - !Sub 'arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:${DomainName}-repo*'
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          GITHUB_TOKEN: !Ref GitHubToken
          GITHUB_REPO: !Ref GitHubRepo
          DOMAIN_NAME: !Ref DomainName
      Events:
        AdminCreateGitHubIssueEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/create-github-issue
            Method: POST

  AdminParseDocumentFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-parse-document'
      CodeUri: lambda-functions/admin/
      Handler: parse-document.handler
      Runtime: nodejs22.x
      Timeout: 30
      MemorySize: 512
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Events:
        AdminParseDocumentEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/parse-document
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminToggleQuestionSetFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-toggle-question-set'
      CodeUri: lambda-functions/admin/
      Handler: toggle-question-set.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminToggleQuestionSetEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/toggle-question-set/{setId}
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminToggleQuickstartFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-toggle-quickstart'
      CodeUri: lambda-functions/admin/
      Handler: toggle-quickstart.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminToggleQuickstartEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/toggle-quickstart/{setId}
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminEditQuestionSetFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-edit-question-set'
      CodeUri: lambda-functions/admin/
      Handler: edit-question-set.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminEditQuestionSetEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/edit-question-set/{setId}
            Method: PUT
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminDeleteQuestionSetFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-delete-question-set'
      CodeUri: lambda-functions/admin/
      Handler: delete-question-set.handler
      Runtime: nodejs22.x
      # Deleting a set now sweeps the legacy partition AND every version
      # partition, so a many-versioned set is several times the work it was.
      Timeout: 60
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminDeleteQuestionSetEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/question-sets/{setId}
            Method: DELETE
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Export a question set as CSV/JSON.
  ##
  ## The lambda has existed for a long time but had NO route, so
  ## QuestionSetEditor.jsx's "Download CSV" has been calling a 404. The route is
  ## added here because a lossless download is half of the replace round trip:
  ## download the current version, edit it, upload it back as a new one.
  AdminDownloadQuestionSetFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-download-question-set'
      CodeUri: lambda-functions/admin/
      Handler: download-question-set.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminDownloadQuestionSetEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/download-question-set/{setId}
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Question-set MEDIA
  ##
  ## Two routes, one bucket. The browser uploads STRAIGHT TO S3 with a
  ## presigned PUT — a folder of artwork never passes through API Gateway,
  ## whose payload ceiling is 10 MB and whose per-request cost would be paid to
  ## move bytes nothing here looks at.
  ##
  ## Both are Cognito-authorised like every other /admin/* route, and both call
  ## requireSetManager on top of that: an admin may touch any set's media, a
  ## host only the sets they created. See lambda-functions/auth/authorizer.js —
  ## these two are on HOST_ADMIN_ROUTES for that reason.

  AdminMediaUploadUrlsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-media-upload-urls'
      CodeUri: lambda-functions/admin/
      Handler: media-upload-urls.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          MEDIA_BUCKET: !Ref MediaBucket
      Policies:
        # READ on the table, not Crud. This function reads one SETS row to
        # answer "does it exist and who owns it" and writes nothing anywhere.
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
        # THE WHOLE SECURITY ARGUMENT FOR THE PRESIGNED URL IS THIS STATEMENT.
        # A presigned URL grants the intersection of the signer's permissions
        # and the signed request, so a URL minted by this role can only ever
        # write an object under sets/ in the media bucket — not the website
        # bucket, not the reports bucket, not a key outside the prefix, however
        # the handler is called and whatever it is asked to sign.
        - Statement:
            - Effect: Allow
              Action: ['s3:PutObject']
              Resource: !Sub '${MediaBucket.Arn}/sets/*'
      Events:
        AdminMediaUploadUrlsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/question-sets/{setId}/media/uploads
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminMediaStatusFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-media-status'
      CodeUri: lambda-functions/admin/
      Handler: media-status.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          MEDIA_BUCKET: !Ref MediaBucket
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
        # LIST ONLY, AND ONLY UNDER sets/. Verification never reads an object's
        # bytes — "is the image in the right place", not "is it a valid image"
        # — so s3:GetObject is not granted and could not be used if the handler
        # were changed to try.
        - Statement:
            - Effect: Allow
              Action: ['s3:ListBucket']
              Resource: !GetAtt MediaBucket.Arn
              Condition:
                StringLike:
                  's3:prefix': ['sets/*']
      Events:
        AdminMediaStatusEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/question-sets/{setId}/media
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Question-set VERSIONING
  ##
  ## A set's questions live at PK=SET#<id>#v<n>; the SETS metadata row carries
  ## activeVersion and versions[]. These three routes list, promote and delete
  ## versions. See docs/superpowers/specs/2026-08-08-question-set-versioning-design.md.
  ##
  ## Cognito-authorised, mirroring every other /admin/* route.

  AdminGetSetVersionsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-get-set-versions'
      CodeUri: lambda-functions/admin/
      Handler: get-set-versions.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminGetSetVersionsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/question-sets/{setId}/versions
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminPromoteSetVersionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-promote-set-version'
      CodeUri: lambda-functions/admin/
      Handler: promote-set-version.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminPromoteSetVersionEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/question-sets/{setId}/versions/{version}/promote
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminDeleteSetVersionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-delete-set-version'
      CodeUri: lambda-functions/admin/
      Handler: delete-set-version.handler
      Runtime: nodejs22.x
      # A version is one partition (up to ~160 question rows plus categories),
      # deleted in retried batches of 25 — the same budget the whole-set delete
      # gets, for a strictly smaller job.
      Timeout: 60
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        AdminDeleteSetVersionEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/question-sets/{setId}/versions/{version}
            Method: DELETE
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## Admin Game Management Functions
  AdminClearGameFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-clear-game'
      CodeUri: lambda-functions/admin/
      Handler: delete-game.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Events:
        AdminClearGameEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/clear-game/{gameId}
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminUpdateGameCategoriesFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-update-game-categories'
      CodeUri: lambda-functions/admin/
      Handler: update-game-categories.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Events:
        AdminUpdateGameCategoriesEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/games/{gameId}/categories
            Method: PUT
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminClearAllGamesFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-clear-all-games'
      CodeUri: lambda-functions/admin/
      Handler: clear-all-games.handler
      Runtime: nodejs22.x
      Timeout: 300
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Events:
        AdminClearAllGamesEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/clear-all-games
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminDownloadTemplateFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-download-template'
      CodeUri: lambda-functions/admin/
      Handler: download-template.handler
      Runtime: nodejs22.x
      Timeout: 30
      Events:
        AdminDownloadTemplateEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/download-template
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ## AI Prompt Management Functions
  AdminGetAIPromptsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-get-ai-prompts'
      CodeUri: lambda-functions/admin/
      Handler: get-ai-prompts.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3CrudPolicy:
            BucketName: !Ref AIPromptsBucket
      Events:
        AdminGetAIPromptsEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-prompts
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # The persona library. Personas share the AIPROMPTS partition but sit under
  # SK='PERSONA#', which AdminGetAIPromptsFunction hard-filters out — so without
  # this route there is no way to read them at all (D8).
  AdminGetPersonasFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-get-personas'
      CodeUri: lambda-functions/admin/
      Handler: get-personas.handler
      Runtime: nodejs22.x
      Timeout: 30
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
      Events:
        AdminGetPersonasEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/personas
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminCreateAIPromptFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-create-ai-prompt'
      CodeUri: lambda-functions/admin/
      Handler: create-ai-prompt.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3CrudPolicy:
            BucketName: !Ref AIPromptsBucket
      Events:
        AdminCreateAIPromptEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-prompts
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        AdminSaveAIPromptEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-prompts/save
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminUpdateAIPromptFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-update-ai-prompt'
      CodeUri: lambda-functions/admin/
      Handler: update-ai-prompt.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3CrudPolicy:
            BucketName: !Ref AIPromptsBucket
      Events:
        AdminUpdateAIPromptEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-prompts/{promptId}
            Method: PUT
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminDeleteAIPromptFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-delete-ai-prompt'
      CodeUri: lambda-functions/admin/
      Handler: delete-ai-prompt.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3CrudPolicy:
            BucketName: !Ref AIPromptsBucket
      Events:
        AdminDeleteAIPromptEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-prompts/{promptId}
            Method: DELETE
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminAIPromptAdvisorFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-prompt-advisor'
      CodeUri: lambda-functions/admin/
      Handler: ai-prompt-advisor.handler
      Runtime: nodejs22.x
      Timeout: 120
      MemorySize: 512
      Environment:
        Variables:
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3CrudPolicy:
            BucketName: !Ref AIPromptsBucket
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
      Events:
        AdminAIPromptAdvisorEvent:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-prompt-advisor
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminAIGeneratePromptFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-ai-generate-prompt'
      CodeUri: lambda-functions/admin/
      Handler: ai-generate-prompt.handler
      Runtime: nodejs22.x
      Timeout: 60
      MemorySize: 512
      Policies:
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - bedrock:InvokeModel
                - bedrock:InvokeModelWithResponseStream
              Resource:
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-sonnet-4-6'
                - !Sub 'arn:aws:bedrock:${AWS::Region}:${AWS::AccountId}:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-sonnet-4-6'
                - 'arn:aws:bedrock:us-east-1::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-east-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
                - 'arn:aws:bedrock:us-west-2::foundation-model/anthropic.claude-haiku-4-5-20251001-v1:0'
      Events:
        AIGeneratePrompt:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/ai-generate-prompt
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminPopulateDefaultsFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-populate-defaults'
      CodeUri: lambda-functions/admin/
      Handler: populate-defaults.handler
      Runtime: nodejs22.x
      Timeout: 60
      MemorySize: 512
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - S3CrudPolicy:
            BucketName: !Ref AIPromptsBucket
      Events:
        PopulateDefaults:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/populate-defaults
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminExportToArchiveFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-export-to-archive'
      CodeUri: lambda-functions/admin/
      Handler: export-to-archive.handler
      Runtime: nodejs22.x
      Timeout: 300
      MemorySize: 512
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          ARCHIVE_SERVICE_URL: 'https://archive.seibtribe.us'
          # THE REASON EVERY ARCHIVED PROMPT WAS EMPTY.
          #
          # A prompt is a two-store record: the pointer row in DynamoDB, and the
          # BODY in this bucket at `s3Key`. export-to-archive.js reads
          # `process.env.AI_PROMPTS_BUCKET` to fetch that body — and this
          # function had neither the variable nor the S3 read below, so the
          # fetch failed on every prompt, every time. The old code swallowed
          # that into `promptContent = {}` and uploaded a metadata-only shell
          # with a 200, so nine prompts sat in the shared archive with
          # instructions, outputFormat, template and scenario all "" while the
          # console reported nine successful exports.
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        # Read-only: an export reads bodies, it never writes one.
        - S3ReadPolicy:
            BucketName: !Ref AIPromptsBucket
      Events:
        ExportToArchive:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/export-to-archive
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminImportFromArchiveFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-import-from-archive'
      CodeUri: lambda-functions/admin/
      Handler: import-from-archive.handler
      Runtime: nodejs22.x
      Timeout: 300
      MemorySize: 512
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          ARCHIVE_SERVICE_URL: 'https://archive.seibtribe.us'
          # The mirror of the export function's variable, and needed for the
          # same structural reason: importing a prompt means WRITING a body into
          # this bucket and pointing the new row's `s3Key` at it. An import that
          # only writes the DynamoDB row produces a prompt every reader treats
          # as bodyless — which is what import-from-archive.js did.
          AI_PROMPTS_BUCKET: !Ref AIPromptsBucket
      Policies:
        # Tenant content is encrypted per organisation. This function reads or
        # writes some, so it needs to unwrap that org's data key — and ONLY with
        # an orgId encryption context, which the key policy makes mandatory.
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action: [ kms:Decrypt ]
              Resource: !GetAtt TenantKey.Arn
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - s3:GetObject
              Resource:
                - 'arn:aws:s3:::engage2-archive-content/*'
        # Crud, not Read: this one puts the body it just downloaded.
        - S3CrudPolicy:
            BucketName: !Ref AIPromptsBucket
      Events:
        ImportFromArchive:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/import-from-archive
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  AdminListLocalArchiveFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-admin-list-local-archive'
      CodeUri: lambda-functions/admin/
      Handler: list-local-archive.handler
      Runtime: nodejs22.x
      Timeout: 30
      MemorySize: 256
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
      Events:
        ListLocalArchive:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/list-local-archive
            Method: GET
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  ### S3 Buckets with Simple Naming
  FrontendBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${StackName}-web'
      WebsiteConfiguration:
        IndexDocument: index.html
        ErrorDocument: error.html
      PublicAccessBlockConfiguration:
        BlockPublicAcls: false
        BlockPublicPolicy: false
        IgnorePublicAcls: false
        RestrictPublicBuckets: false
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: StackName
          Value: !Ref StackName

  ReportsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${StackName}-reports'
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      LifecycleConfiguration:
        Rules:
          - Id: DeleteOldReports
            Status: Enabled
            ExpirationInDays: 90
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: StackName
          Value: !Ref StackName

  ### QUESTION-SET MEDIA — AND WHY IT IS NOT THE WEBSITE BUCKET
  ###
  ### Every buildspec publishes the frontend with
  ###
  ###     aws s3 sync dist/ s3://$BUCKET_NAME/ --delete
  ###
  ### (buildspec-dev.yml:104, buildspec-test.yml:110, buildspec-prod.yml:116).
  ### `--delete` removes every object in the destination that is not in dist/.
  ### Uploaded question artwork under `sets/<setId>/` is never in dist/ — the
  ### committed repo assets under /assets/art/ survive precisely because they
  ### ARE — so images kept in FrontendBucket would live until the next deploy
  ### of that tier and then disappear, mid-season, with no error anywhere.
  ###
  ### `--exclude "sets/*"` on the sync would also work (the filter is applied
  ### to the destination listing, so excluded keys are not deletion
  ### candidates). It was rejected: it is an unremarkable flag in three files
  ### that a copy-paste loses, it does nothing for a hand-run sync, and its
  ### absence is invisible until a room is looking at a broken image. A
  ### SEPARATE BUCKET cannot be reached by a sync aimed at the website bucket
  ### whatever flags that sync carries.
  ###
  ### The images stay SAME-ORIGIN anyway: the CloudFront distribution below
  ### serves `sets/*` from this bucket under the site's own hostname. That is
  ### what lets the stored value keep going straight into <img src> —
  ### PlayerPage.jsx:1961,2196 and GameHostPage.jsx:4571,4614,5213 — with no
  ### resolver and no change to any of those call sites.
  ###
  ### It is deliberately NOT conditional on HasDomain: the presign and
  ### verification Lambdas need a bucket in every stack, domain or not.
  MediaBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${StackName}-media'
      # Public read, exactly like FrontendBucket, because the same CloudFront
      # origin configuration is used (S3OriginConfig with an empty
      # OriginAccessIdentity) and because these images are shown to players who
      # never sign in. WRITE is not public: it needs a presigned PUT, which
      # only the admin Lambda's role can mint.
      PublicAccessBlockConfiguration:
        BlockPublicAcls: false
        BlockPublicPolicy: false
        IgnorePublicAcls: false
        RestrictPublicBuckets: false
      CorsConfiguration:
        CorsRules:
          # The browser PUTs straight to S3, so the bucket must answer the
          # preflight. This grants no access — the presigned URL does that.
          - AllowedMethods: [GET, HEAD, PUT]
            AllowedOrigins: !If
              - HasDomain
              - [!Sub 'https://${DomainName}']
              - ['*']
            AllowedHeaders: ['*']
            ExposedHeaders: [ETag]
            MaxAge: 3000
      LifecycleConfiguration:
        Rules:
          # A failed multipart upload otherwise bills forever and is invisible
          # in the console's object list.
          - Id: AbandonAbortedUploads
            Status: Enabled
            AbortIncompleteMultipartUpload:
              DaysAfterInitiation: 3
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: StackName
          Value: !Ref StackName

  MediaBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref MediaBucket
      PolicyDocument:
        Statement:
          - Sid: PublicReadGetObject
            Effect: Allow
            Principal: '*'
            Action: 's3:GetObject'
            Resource: !Sub '${MediaBucket.Arn}/*'

  AIPromptsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub '${StackName}-ai-prompts'
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true
      VersioningConfiguration:
        Status: Enabled
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: StackName
          Value: !Ref StackName

  ### S3 Bucket Policy
  FrontendBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref FrontendBucket
      PolicyDocument:
        Statement:
          - Sid: PublicReadGetObject
            Effect: Allow
            Principal: '*'
            Action: 's3:GetObject'
            Resource: !Sub '${FrontendBucket.Arn}/*'

  ### CloudFront Distribution (if domain is provided)
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Condition: HasDomain
    Properties:
      DistributionConfig:
        Aliases:
          - !Ref DomainName
        Origins:
          - Id: S3Origin
            DomainName: !GetAtt FrontendBucket.RegionalDomainName
            S3OriginConfig:
              OriginAccessIdentity: ''
          # Question-set artwork. A SECOND ORIGIN rather than a second hostname,
          # so `sets/<setId>/x.jpg` stored on a question stays a same-origin
          # relative URL and <img src={question.image}> keeps working unchanged.
          - Id: MediaOrigin
            DomainName: !GetAtt MediaBucket.RegionalDomainName
            S3OriginConfig:
              OriginAccessIdentity: ''
        DefaultCacheBehavior:
          TargetOriginId: S3Origin
          ViewerProtocolPolicy: redirect-to-https
          AllowedMethods: [GET, HEAD]
          CachedMethods: [GET, HEAD]
          CachePolicyId: 4135ea2d-6df8-44a3-9df3-4b5a84be39ad # CachingDisabled
        CacheBehaviors:
          # THE PATH THAT KEEPS THE IMAGES OUT OF THE DEPLOY'S REACH. Everything
          # under sets/ comes from MediaBucket, which no `aws s3 sync dist/
          # s3://${StackName}-web --delete` can touch. Removing this behaviour
          # does not break the site quietly — it 404s every uploaded image at
          # once — and __tests__/setMediaStorage.test.js fails if it is removed.
          - PathPattern: 'sets/*'
            TargetOriginId: MediaOrigin
            ViewerProtocolPolicy: redirect-to-https
            AllowedMethods: [GET, HEAD]
            CachedMethods: [GET, HEAD]
            # CachingOptimized, unlike the default behaviour's CachingDisabled:
            # the app bundle changes on every deploy and must not be cached, an
            # image at a given key does not change at all. A replace writes the
            # same key, so a replace is followed by the deploy-time
            # invalidation or by a hard refresh — stated in the media panel.
            CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6
        Enabled: true
        DefaultRootObject: index.html
        CustomErrorResponses:
          - ErrorCode: 403
            ResponseCode: 200
            ResponsePagePath: /index.html
          - ErrorCode: 404
            ResponseCode: 200
            ResponsePagePath: /index.html
        ViewerCertificate:
          AcmCertificateArn: !Ref SSLCertificate
          SslSupportMethod: sni-only
        PriceClass: PriceClass_100
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: StackName
          Value: !Ref StackName



  ### SSL Certificate (if domain is provided)
  SSLCertificate:
    Type: AWS::CertificateManager::Certificate
    Condition: HasDomain
    Properties:
      DomainName: !Ref DomainName
      ValidationMethod: DNS
      DomainValidationOptions:
        - DomainName: !Ref DomainName
          HostedZoneId: !Ref HostedZoneId
      Tags:
        - Key: Environment
          Value: !Ref Environment
        - Key: StackName
          Value: !Ref StackName

  ### Route53 Record (if domain is provided)
  DNSRecord:
    Type: AWS::Route53::RecordSet
    Condition: HasDomain
    Properties:
      HostedZoneId: !Ref HostedZoneId
      Name: !Ref DomainName
      Type: A
      AliasTarget:
        DNSName: !GetAtt CloudFrontDistribution.DomainName
        HostedZoneId: Z2FDTNDATAQYW2  # CloudFront hosted zone ID

  ### WebSocket Routes
  ConnectRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref WebSocketApi
      RouteKey: $connect
      AuthorizationType: NONE
      OperationName: ConnectRoute
      Target: !Join
        - '/'
        - - 'integrations'
          - !Ref ConnectInteg

  DisconnectRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref WebSocketApi
      RouteKey: $disconnect
      AuthorizationType: NONE
      OperationName: DisconnectRoute
      Target: !Join
        - '/'
        - - 'integrations'
          - !Ref DisconnectInteg

  MessageRoute:
    Type: AWS::ApiGatewayV2::Route
    Properties:
      ApiId: !Ref WebSocketApi
      RouteKey: $default
      AuthorizationType: NONE
      OperationName: MessageRoute
      Target: !Join
        - '/'
        - - 'integrations'
          - !Ref MessageInteg

  ### WebSocket Integrations
  ConnectInteg:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref WebSocketApi
      Description: Connect Integration
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${ConnectFunction.Arn}/invocations'

  DisconnectInteg:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref WebSocketApi
      Description: Disconnect Integration
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${DisconnectFunction.Arn}/invocations'

  MessageInteg:
    Type: AWS::ApiGatewayV2::Integration
    Properties:
      ApiId: !Ref WebSocketApi
      Description: Message Integration
      IntegrationType: AWS_PROXY
      IntegrationUri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MessageFunction.Arn}/invocations'

  ### Lambda Permissions
  ConnectPermission:
    Type: AWS::Lambda::Permission
    DependsOn:
      - WebSocketApi
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref ConnectFunction
      Principal: apigateway.amazonaws.com

  DisconnectPermission:
    Type: AWS::Lambda::Permission
    DependsOn:
      - WebSocketApi
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref DisconnectFunction
      Principal: apigateway.amazonaws.com

  MessagePermission:
    Type: AWS::Lambda::Permission
    DependsOn:
      - WebSocketApi
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref MessageFunction
      Principal: apigateway.amazonaws.com

  ### Cognito Resources for Authentication
  UserPoolV2:
    Type: AWS::Cognito::UserPool
    Properties:
      UserPoolName: !Sub '${StackName}-users'
      AutoVerifiedAttributes:
        - email
      UsernameAttributes:
        - email
      Schema:
        - Name: email
          AttributeDataType: String
          Required: true
          Mutable: true
        - Name: name
          AttributeDataType: String
          Required: true
          Mutable: true
        - Name: status
          AttributeDataType: String
          Mutable: true
        - Name: role
          AttributeDataType: String
          Mutable: true
      Policies:
        PasswordPolicy:
          MinimumLength: 8
          RequireUppercase: true
          RequireLowercase: true
          RequireNumbers: true
          RequireSymbols: true
      # THE EMAIL A PERSON ACTUALLY RECEIVES.
      #
      # Reported: "even in test it is generic and there is nothing reference that
      # it is for engage. it would be nice to use the domain and also a bit of
      # info around what it really is for."
      #
      # There was no VerificationMessageTemplate here at all, so both pools sent
      # Cognito's stock wording: a bare six-digit code, no product name, no
      # domain, and nothing saying what the code unlocks. Someone who requested
      # a reset an hour earlier could not tell it apart from a phishing attempt.
      #
      # ONE TEMPLATE COVERS BOTH JOURNEYS. With DefaultEmailOption
      # CONFIRM_WITH_CODE, Cognito uses EmailMessage/EmailSubject for sign-up
      # verification AND for forgot-password. {####} is the code and is the one
      # token Cognito substitutes — it MUST appear in the body or the stack
      # fails to deploy, which is the guardrail that stops a well-meaning
      # rewrite from shipping an email with no code in it.
      #
      # The domain is interpolated rather than hardcoded so dev, test and prod
      # each name themselves. `DomainName` defaults to "" — the !If keeps the
      # sentence grammatical when it is unset rather than printing "at .".
      VerificationMessageTemplate:
        DefaultEmailOption: CONFIRM_WITH_CODE
        EmailSubject: !Sub 'Your Engage verification code (${StackName})'
        EmailMessage: !If
          - HasDomain
          - !Sub |
              <p>Someone asked to verify an email address or reset a password for
              <strong>Engage</strong> at <a href="https://${DomainName}">${DomainName}</a>.</p>
              <p>Your code is <strong>{####}</strong></p>
              <p>Enter it on the sign-in screen to finish. The code expires shortly.</p>
              <p>If this wasn't you, no action is needed — the code is useless on its own
              and nothing has changed on your account.</p>
          - !Sub |
              <p>Someone asked to verify an email address or reset a password for
              <strong>Engage</strong> (${StackName}).</p>
              <p>Your code is <strong>{####}</strong></p>
              <p>Enter it on the sign-in screen to finish. The code expires shortly.</p>
              <p>If this wasn't you, no action is needed — the code is useless on its own
              and nothing has changed on your account.</p>
      # THE ACCOUNT THE EMAIL LEAVES THROUGH.
      #
      # Without this block the pool sends via COGNITO_DEFAULT: a shared Cognito
      # sender with a hard daily cap (~50/day), a no-reply@verificationemail.com
      # From address no one recognises, and no DKIM tied to our domain — which
      # is exactly the "is this phishing?" half of the generic-email report the
      # VerificationMessageTemplate above fixed the wording half of.
      #
      # DEVELOPER routes the same messages through this account's SES instead.
      # Verified before this shipped (2026-08-23): the seibtribe.us DOMAIN
      # identity is Verified=true with DKIM SUCCESS, and the account has SES
      # production access (50k/day) — NOT sandbox, which would silently refuse
      # every unverified recipient and break password reset for everyone.
      # Same-account SES needs no sender-authorization policy; Cognito uses its
      # email service-linked role. The From address needs no separate identity —
      # the domain identity covers every address at seibtribe.us.
      EmailConfiguration:
        EmailSendingAccount: DEVELOPER
        SourceArn: !Sub 'arn:aws:ses:${AWS::Region}:${AWS::AccountId}:identity/seibtribe.us'
        From: 'Engage <no-reply@seibtribe.us>'
      # PostConfirmation trigger will be configured after deployment to avoid circular dependency
      UserPoolTags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  UserPoolClient:
    Type: AWS::Cognito::UserPoolClient
    Properties:
      ClientName: !Sub '${StackName}-web-client'
      UserPoolId: !Ref UserPoolV2
      GenerateSecret: false
      # !Ref on a UserPoolIdentityProvider returns its ProviderName ("Google")
      # AND creates the implicit dependency that orders provider creation
      # before this client update (a literal "Google" races and fails).
      SupportedIdentityProviders: !If
        - HasGoogleOAuth
        - [COGNITO, !Ref GoogleIdentityProvider]
        - [COGNITO]
        # Social providers commented out - configure manually in Cognito Console after deployment
        # - Facebook  
        # - LoginWithAmazon
        # - SignInWithApple
      CallbackURLs:
        - !If 
          - HasDomain
          - !Sub 'https://${DomainName}/auth/callback'
          - !Sub 'http://localhost:3000/auth/callback'
        - !If
          - HasSecondaryDomain
          - !Sub 'https://${SecondaryDomainName}/auth/callback'
          - !Ref AWS::NoValue
      LogoutURLs:
        - !If 
          - HasDomain
          - !Sub 'https://${DomainName}'
          - !Sub 'http://localhost:3000'
        - !If
          - HasSecondaryDomain
          - !Sub 'https://${SecondaryDomainName}'
          - !Ref AWS::NoValue
      AllowedOAuthFlows:
        - code
        - implicit
      AllowedOAuthScopes:
        - email
        - openid
        - profile
        - aws.cognito.signin.user.admin
      AllowedOAuthFlowsUserPoolClient: true
      PreventUserExistenceErrors: ENABLED
      ExplicitAuthFlows:
        - ALLOW_USER_PASSWORD_AUTH
        - ALLOW_USER_SRP_AUTH
        - ALLOW_REFRESH_TOKEN_AUTH
      # Explicit (matches the default) — also forces CloudFormation to
      # re-apply this resource and correct live drift: the deployed client
      # had lost its ExplicitAuthFlows to a manual console edit.
      EnableTokenRevocation: true
      RefreshTokenValidity: 30
      AccessTokenValidity: 1
      IdTokenValidity: 1
      TokenValidityUnits:
        RefreshToken: days
        AccessToken: hours
        IdToken: hours

  UserPoolDomainV2:
    Type: AWS::Cognito::UserPoolDomain
    Properties:
      Domain: !Sub '${StackName}-auth-v2'
      UserPoolId: !Ref UserPoolV2

  # User Groups
  AdminUserGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: admins
      Description: Administrator users with full access
      UserPoolId: !Ref UserPoolV2
      Precedence: 1

  HostUserGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: hosts
      Description: Host users who can create and manage games
      UserPoolId: !Ref UserPoolV2
      Precedence: 2

  PendingUserGroup:
    Type: AWS::Cognito::UserPoolGroup
    Properties:
      GroupName: pending
      Description: Users awaiting approval
      UserPoolId: !Ref UserPoolV2
      Precedence: 3

  # Google Identity Provider
  GoogleIdentityProvider:
    Type: AWS::Cognito::UserPoolIdentityProvider
    Condition: HasGoogleOAuth
    Properties:
      UserPoolId: !Ref UserPoolV2
      ProviderName: Google
      ProviderType: Google
      ProviderDetails:
        client_id: !Ref GoogleClientId
        client_secret: !Ref GoogleClientSecret
        authorize_scopes: email openid profile
      AttributeMapping:
        email: email
        name: name
        username: sub

# Temporarily commented out Identity Pool to resolve circular dependency
  # # Identity Pool for authenticated and unauthenticated access
  # IdentityPool:
  #   Type: AWS::Cognito::IdentityPool
  #   Properties:
  #     IdentityPoolName: !Sub '${StackName}-identity-pool'
  #     AllowUnauthenticatedIdentities: true
  #     CognitoIdentityProviders:
  #       - ClientId: !Ref UserPoolClient
  #         ProviderName: !GetAtt UserPool.ProviderName

# # IAM Roles for Identity Pool
  # CognitoAuthenticatedRole:
  #   Type: AWS::IAM::Role
  #   Properties:
  #     RoleName: !Sub '${StackName}-cognito-authenticated'
  #     AssumeRolePolicyDocument:
  #       Version: '2012-10-17'
  #       Statement:
  #         - Effect: Allow
  #           Principal:
  #             Federated: cognito-identity.amazonaws.com
  #           Action: sts:AssumeRoleWithWebIdentity
  #           Condition:
  #             StringEquals:
  #               cognito-identity.amazonaws.com:aud: !Ref IdentityPool
  #             ForAnyValue:StringLike:
  #               cognito-identity.amazonaws.com:amr: authenticated
  #     Policies:
  #       - PolicyName: AuthenticatedUserPolicy
  #         PolicyDocument:
  #           Version: '2012-10-17'
  #           Statement:
  #             - Effect: Allow
  #               Action:
  #                 - execute-api:Invoke
  #               Resource:
  #                 - !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${RestApi}/*/*/*'
  #                 - !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*'

  # CognitoUnauthenticatedRole:
  #   Type: AWS::IAM::Role
  #   Properties:
  #     RoleName: !Sub '${StackName}-cognito-unauthenticated'
  #     AssumeRolePolicyDocument:
  #       Version: '2012-10-17'
  #       Statement:
  #         - Effect: Allow
  #           Principal:
  #             Federated: cognito-identity.amazonaws.com
  #           Action: sts:AssumeRoleWithWebIdentity
  #           Condition:
  #             StringEquals:
  #               cognito-identity.amazonaws.com:aud: !Ref IdentityPool
  #             ForAnyValue:StringLike:
  #               cognito-identity.amazonaws.com:amr: unauthenticated
  #     Policies:
  #       - PolicyName: UnauthenticatedUserPolicy
  #         PolicyDocument:
  #           Version: '2012-10-17'
  #           Statement:
  #             - Effect: Allow
  #               Action:
  #                 - execute-api:Invoke
  #               Resource:
  #                 # Only allow player endpoints
  #                 - !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${RestApi}/*/GET/games/*'
  #                 - !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${RestApi}/*/POST/games/*/join'
  #                 - !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${RestApi}/*/POST/games/*/answer'
  #                 - !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${RestApi}/*/POST/games/*/vote'
  #                 - !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${WebSocketApi}/*'

  # # Attach roles to Identity Pool
  # IdentityPoolRoleAttachment:
  #   Type: AWS::Cognito::IdentityPoolRoleAttachment
  #   Properties:
  #     IdentityPoolId: !Ref IdentityPool
  #     Roles:
  #       authenticated: !GetAtt CognitoAuthenticatedRole.Arn
  #       unauthenticated: !GetAtt CognitoUnauthenticatedRole.Arn
  #     RoleMappings:
  #       CognitoProvider:
  #         IdentityProvider: !Sub '${UserPool.ProviderName}:${UserPoolClient}'
  #         Type: Token
  #         AmbiguousRoleResolution: AuthenticatedRole

  # Lambda Authorizer for the HTTP API (validates UserPoolV2 JWTs).
  # Payload format 2.0 simple responses — see lambda-functions/auth/authorizer.js.
  AuthorizerFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-authorizer'
      CodeUri: lambda-functions/auth/
      Handler: authorizer.handler
      Runtime: nodejs22.x
      Environment:
        Variables:
          USER_POOL_ID: !Ref UserPoolV2
          CLIENT_ID: !Ref UserPoolClient
          REGION: !Ref AWS::Region
          # The authorizer now reads the caller's organisation memberships
          # (PK=USER#<sub>, begins_with(SK,'ORG#')) and their PROFILE row, and
          # puts orgId/orgRole/orgIds into the context every handler reads.
          TABLE_NAME: !Ref GameTable
      Policies:
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - cognito-idp:AdminListGroupsForUser
                - cognito-idp:AdminGetUser
              Resource: !GetAtt UserPoolV2.Arn
        # READ ONLY, and only the membership rows are ever read. The authorizer
        # runs on EVERY authenticated request, so this is the hottest IAM grant
        # in the stack; it gets Query and GetItem and nothing else.
        #
        # WITHOUT THIS THE FAILURE IS SILENT. authorizer.js resolves the org
        # context defensively — a throttle, a torn row or a missing table all
        # degrade to `orgId: ''` and still authorize, because a caller who has
        # not joined a team yet is a legitimate state and must not be locked
        # out. That is right, and it means a MISSING POLICY presents as "the
        # org features quietly do nothing" rather than as an error. The only
        # trace is a console.error. Wave 5's smoke test asserts a real token
        # comes back carrying an orgId for that reason.
        - DynamoDBReadPolicy:
            TableName: !Ref GameTable
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # SAM does NOT auto-create the invoke permission for HttpApi Lambda
  # authorizers — without this, API Gateway gets AccessDenied and every
  # authorized route returns 500. (This was likely the original "circular
  # dependency" mystery; the permission is a leaf resource, so no cycle.)
  AuthorizerInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref AuthorizerFunction
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub 'arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${RestApi}/authorizers/*'

  # User Management Lambda
  UserManagementFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-user-management'
      CodeUri: lambda-functions/admin/
      Handler: manage-users.handler
      Runtime: nodejs22.x
      Timeout: 60
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          USER_POOL_ID: !Ref UserPoolV2
          REGION: !Ref AWS::Region
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - cognito-idp:ListUsers
                - cognito-idp:AdminGetUser
                - cognito-idp:AdminListGroupsForUser
                - cognito-idp:AdminEnableUser
                - cognito-idp:AdminDisableUser
                - cognito-idp:AdminDeleteUser
                - cognito-idp:AdminUpdateUserAttributes
                - cognito-idp:AdminAddUserToGroup
                - cognito-idp:AdminRemoveUserFromGroup
              Resource: !GetAtt UserPoolV2.Arn
      Events:
        ListUsers:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/users/list
            Method: POST
            Auth:
              Authorizer: CognitoAuthorizer
        ChangeUserState:
          Type: HttpApi
          Properties:
            ApiId: !Ref RestApi
            Path: /admin/users/{username}/state
            Method: PUT
            Auth:
              Authorizer: CognitoAuthorizer
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # Post-confirmation Lambda for user registration workflow
  PostConfirmationFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub '${StackName}-post-confirmation'
      CodeUri: lambda-functions/auth/
      Handler: post-confirmation.handler
      Runtime: nodejs22.x
      Timeout: 30
      Environment:
        Variables:
          TABLE_NAME: !Ref GameTable
          USER_POOL_ID: !Ref UserPoolV2
          REGION: !Ref AWS::Region
      Policies:
        - DynamoDBCrudPolicy:
            TableName: !Ref GameTable
        - Version: '2012-10-17'
          Statement:
            - Effect: Allow
              Action:
                - cognito-idp:AdminGetUser
                - cognito-idp:AdminListGroupsForUser
                - cognito-idp:AdminAddUserToGroup
                - cognito-idp:AdminUpdateUserAttributes
              Resource: !GetAtt UserPoolV2.Arn
      Tags:
        Environment: !Ref Environment
        StackName: !Ref StackName

  # Lambda permission for the post-confirmation trigger. The trigger itself
  # (UserPoolV2 LambdaConfig -> PostConfirmationFunction) cannot live in the
  # template without a circular dependency, so it is attached post-deploy by
  # scripts/setup-post-confirmation.sh; this permission is safe to keep here.
  PostConfirmationLambdaPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref PostConfirmationFunction
      Principal: cognito-idp.amazonaws.com
      SourceArn: !GetAtt UserPoolV2.Arn

Outputs:
  # API Endpoints
  RestApiUrl:
    Description: REST API Gateway endpoint URL
    Value: !Sub 'https://${RestApi}.execute-api.${AWS::Region}.amazonaws.com/${Environment}'
    Export:
      Name: !Sub '${StackName}-rest-api-url'

  WebSocketUrl:
    Description: WebSocket API Gateway endpoint URL
    Value: !Sub 'wss://${WebSocketApi}.execute-api.${AWS::Region}.amazonaws.com/${Environment}'
    Export:
      Name: !Sub '${StackName}-websocket-url'

  # Frontend
  FrontendBucketName:
    Description: Frontend S3 bucket name
    Value: !Ref FrontendBucket
    Export:
      Name: !Sub '${StackName}-frontend-bucket'

  ## Named separately from FrontendBucketName ON PURPOSE. The buildspecs read
  ## FrontendBucketName and sync dist/ into it with --delete; a deploy that ever
  ## resolved that name to the media bucket would erase every uploaded image on
  ## the spot. __tests__/setMediaStorage.test.js asserts no buildspec's
  ## --delete sync reads this output.
  MediaBucketName:
    Description: Question-set media S3 bucket name (uploaded question images)
    Value: !Ref MediaBucket
    Export:
      Name: !Sub '${StackName}-media-bucket'

  FrontendUrl:
    Description: Frontend URL
    Value: !If
      - HasDomain
      - !Sub 'https://${DomainName}'
      - !Sub 'http://${FrontendBucket}.s3-website-${AWS::Region}.amazonaws.com'
    Export:
      Name: !Sub '${StackName}-frontend-url'

  # The deploy buildspecs look this up to invalidate the CDN after `s3 sync`.
  # Without it the lookup returned "" and `aws cloudfront create-invalidation
  # --distribution-id` failed with exit 252, marking otherwise-successful dev
  # deploys as Failed. Conditional because the distribution itself is HasDomain.
  CloudFrontDistributionId:
    Condition: HasDomain
    Description: CloudFront distribution ID (used to invalidate cache on deploy)
    Value: !Ref CloudFrontDistribution
    Export:
      Name: !Sub '${StackName}-cloudfront-distribution-id'

  # Database
  GameTableName:
    Description: DynamoDB table name
    Value: !Ref GameTable
    Export:
      Name: !Sub '${StackName}-table-name'

  # Storage
  AIPromptsBucketName:
    Description: AI Prompts S3 bucket name
    Value: !Ref AIPromptsBucket
    Export:
      Name: !Sub '${StackName}-ai-prompts-bucket'

  # Environment Info
  Environment:
    Description: Environment name
    Value: !Ref Environment
    Export:
      Name: !Sub '${StackName}-environment'

  StackName:
    Description: CloudFormation stack name
    Value: !Ref AWS::StackName
    Export:
      Name: !Sub '${StackName}-stack-name'

  # Cognito Outputs
  UserPoolId:
    Description: Cognito User Pool ID (copy to src/.env as REACT_APP_USER_POOL_ID)
    Value: !Ref UserPoolV2
    Export:
      Name: !Sub '${StackName}-user-pool-id'

  UserPoolClientId:
    Description: Cognito User Pool Client ID (copy to src/.env as REACT_APP_CLIENT_ID)
    Value: !Ref UserPoolClient
    Export:
      Name: !Sub '${StackName}-user-pool-client-id'

  UserPoolDomain:
    Description: Cognito User Pool Domain
    Value: !Sub '${StackName}-auth-v2'
    Export:
      Name: !Sub '${StackName}-user-pool-domain'

  # IdentityPoolId:
  #   Description: Cognito Identity Pool ID
  #   Value: !Ref IdentityPool
  #   Export:
  #     Name: !Sub '${StackName}-identity-pool-id'
