# Handoff — 2026-09-21/22 session

Read this first, then `CLAUDE.md` (the deploy rule at the top is load-bearing), then the
memory index the session was working from. Everything below is verified against the repo
at `origin/dev` = `af87e458`.

## 1. Where the three tiers stand

| Tier | Head | State |
|---|---|---|
| dev | `af87e458` | Deployed. The front page is code-split into `351.bundle.js`, not `bundle.js` — grep the chunk, not the bundle, when checking a deploy. |
| test | `1ba58d39` (merge of dev `af87e458`) | Deployed; tree identical to dev. |
| prod | `1ba58d39` (fast-forward of test) | Pipeline started; **halted at `ApprovalForProd`**, waiting for the owner. An earlier prod execution at `1382980d` was also started and is superseded. |

The owner has **not looked at test** for most of this work. Before approving prod, someone
should walk test with an org owner account and a platform `admins` account (see §4).

Promotion mechanics (memory `engage2-promoting-to-test`, `engage2-promoting-to-prod`):
test is **merged into** (`git checkout -B promote-test origin/test && git merge origin/dev`),
prod is a **fast-forward** of test's head (`git push origin origin/test:refs/heads/prod`).
Never force-push either. Both a tag and a branch push deploy; push one, not both.

## 2. The gates, and the exact way to run them

Every push in this session passed all of these on the exact tree pushed:

```bash
# backend — 166 suites; judge by exit code AND suite count; a crashed suite prints nothing
rm -rf .aws-sam; n=0; bad=""; for f in tests/*.js; do case "$f" in *.spec.js) continue;; esac; n=$((n+1)); node "$f" >/dev/null 2>&1 || bad="$bad $f"; done; echo "backend: $n, failed:${bad:- none}"
# frontend — 279 suites / 6855 tests
cd src && CI=true npm test -- --watchAll=false 2>&1 | grep -E "^(Tests|Test Suites):|^FAIL"
npm run lint      # 0 errors, 10 pre-existing warnings
npm run build     # webpack compiles with 2 pre-existing warnings
node tests/no-retired-twin-references.js   # run on STAGED files before every commit
```

`node_modules` is intact in the main checkout; do not `npm install` in a worktree (memory
`engage2-running-the-suites`).

## 3. What shipped this session, by area

### 3.1 Billing — the simulated billing system (ALL FOUR STEPS BUILT)

Design + audit: `docs/handoff/billing-experience-2026-09-22.md` (its STATUS table is
current). Mockups: `docs/design/tenancy-redesign/13–21`. Owner's decisions, all taken:
re-request after decline immediately; percents never stack; downgrade at period end;
invites do not auto-flip the plan; owner-only redeem; `SIM-` invoice numbers.

| Step | Commit | Key files |
|---|---|---|
| 1 Honest foundations | `2ab57fdb` | `admin/get-usage.js` prices with `planFor(orgRow)` and returns `period.resetsOn`; `orgs/create-org.js` refuses `plan` in the body; `GameSetupDialog.jsx` shows the 402 in-dialog (no more `alert()`); `BillingPanel` no longer offers "Create a team" |
| 2 Plan requests | `7f1b3385` | `orgs/plan-requests.js` (5 routes: owner asks / admin reads / owner withdraws / staff queue / staff decide); `components/PlanRequestDialog.jsx` (dialog + 4-state strip), `PlanRequestsPanel.jsx` (platform section `planrequests` with badge), `PlanRequests.css` (`.preq`) |
| 3 Ledger + codes | `fa5729d0` | `shared/pricing-adjust.js` (**pure**, copied to `game/`, frontend imports the game copy; the order of application lives here and nowhere else); `orgs/adjustments.js` (grant/list/revoke, codes create/list/retire, `redeemCodeItems` used INSIDE plan-request approval's transaction); `components/AdjustmentsLedger.jsx` (one list for both sides + `AdjustedBill`), `OrgBillingDrawer.jsx` (drawer, grant, revoke — from Organisations → "Billing…"), `DiscountCodesPanel.jsx` (section `discountcodes`) |
| 4 Invoices | `8ded6991` | `shared/invoices.js` (`closeInvoice` — called from `usage-reconcile.js` nightly for the previous period, conditional put, credits drawn down in-transaction with `CREDIT_APPLIED` ledger rows; **`settle()` is the one Stripe seam**); `get-usage.js` serves `GET /orgs/{id}/invoices[/{period}]` from the frozen rows; `components/InvoicePanel.jsx` (Billing history + the invoice as places inside the Billing section; every `$` is a button with `data-source`) |

Rows (all partitions via `tenant.js`, never a bare literal):
`ORG#<org>/PLANREQ#<ts>#<id>`, `ORGS/PLANREQ#<status>#<ts>#<org>`, `ORG#<org>/ADJ#<ts>#<id>`,
`ORGS/CODE#<CODE>`, `ORG#<org>/CODEUSE#<CODE>`, `ORG#<org>/INVOICE#<yyyy-mm>`,
`ORGS/INVOICE#<yyyy-mm>#<org>`, `ORG#<org>/LEDGER#<p>#PLAN_CHANGE#<req>` and `…#CREDIT_APPLIED#<adj>`.

Authorizer (`auth/authorizer.js`): `platform/plan-requests…`, `platform/orgs/*/adjustments…`,
`platform/codes…` are `admins`; the org-side routes fall through to `ORG_ROUTE` and the
handlers re-check the org role.

Tests: `tests/plan-request-flow.js`, `adjustments-ledger.js`, `pricing-adjust.js`,
`invoice-close.js`, `usage-metering.js`, `org-lifecycle.js`; jest `planRequests`,
`adjustments`, `invoicePanel`, `planRequestsPalette`, `adjustmentsLedgerPalette` (covers
`.adjl .obill .dcode .inv`), `billingPanel`, `gameSetupDialog`.

**Known limits / next:**
- The first invoices exist on the morning of 1 Oct. Nothing is backfilled; do not backfill
  (a backfilled invoice is today's ledger, not a snapshot).
- The request dialog validates a code's *shape* only; the terms are checked at approval.
  A live "this code gives 30% off" preview in the request dialog needs a member-callable
  `checkCode` route (today `checkCode` is only reachable by staff).
- The customer's Plan & usage shows the ledger only when `GET /orgs/{id}/adjustments`
  returns 200 (admin+); members see neither panel, by design.
- `get-usage.js` history rows still report only list totals; the closed-month table on
  Billing history is the adjusted record.
- Not seen in a browser: any of steps 2–4. Both sides need signed-in accounts.

### 3.2 Question sets — create / add / preview

- **New set is a `Modal`** (`components/NewSetDialog.jsx`), content = the unchanged
  `QuestionSetUploadPanel` in `bare` mode. The below-the-table panel is gone.
- **`SetSizeField`** in `CountField.jsx`: categories × questions-in-each, presets 2/3/5/10,
  total stated; used by Trivia and Scenario builders (Poll/Survey keep `CountField`).
- **Add questions** (`components/AddQuestionsDialog.jsx`, `utils/addQuestions.js`,
  `utils/appendMode.js`): one screen — the set as headline, its category balance as bars,
  one sentence for how many, one button that opens the builder **already generating**
  with the set's brief (`briefFromSet`: topic/audience/difficulty read back off the set's
  title and AI-context text; context = the set's name unless a `Context:` was recorded;
  round kind + brief + `customInstruction` for the scenario builder). Rows are appended and
  **saved as a new version the moment "Add N" is pressed** (unsaved only when other edits
  are pending or the set is not yours). The worker receives `appendOnly: true` and creates
  no set (`shared/generated-set.js`). Builders portal to `document.body` (the dusk editor
  made their white cards unreadable).
- The host's set list (`game/get-question-sets.js`) now projects `roundKind`/`roundKindBrief`.
- **No room for a copy:** `admin/get-question-sets.js` returns `setAllowance`; the editor
  refuses on arrival with the numbers and disables every write when a not-yours set has
  nowhere to be copied to.
- Preview card shows the category as the stage's `.kicker` (`QuestionPreview.jsx`).
- `BrandMark.jsx` — the mountain in every top-left corner (a link only where leaving is safe).
- Host's set dialog is dusk and 1120px; editor frame 1280px.
- Queue panel: "Nothing queued — automatic order below"; the dead X is an invisible spacer.

**Next:** the one-screen AI builder (handoff `create-set-experience-2026-09-18.md` Q1),
Survey's AI path (it exports a file and creates nothing), the manual builder, the interview.
Closing the builder on the review screen before pressing Add still discards silently.

### 3.3 Sessions, reports

- **Session TTL** (`session-ttl.js`, copied to `game/` and `websocket/`, held identical by
  `tests/tenant-session-scoping.js` §9): created + 90d on all four rows; `start-game.js`
  rewrites to started + 7d. The 90-day stamp always existed; the rewrite never did — that is
  why August sessions were listed in September. Existing started sessions age out on their
  old 90-day stamp.
- **Reports** (`game/save-report.js` writes `ORG#<org>/REPORTS` rows, Title encrypted with
  the new `reportIndex` crypto entity; bucket lifecycle split: 90d for `retention=standard`
  tagged objects, 365d for `permanent/`). `GET /reports` + `GET /reports/download`
  (`get-reports.js`, `download-saved-report.js`) authorise on the caller's own index row, so
  a report opens after its session is gone. Console section `reports`; host door
  `HostReportsDialog`. Objects saved before this have no row and no tag — they are neither
  listed nor expired. The public "Copy link" URL still dies with the session.

### 3.4 The front page

`docs/design/refresh-2026-09-22/` (rationale, mockups, `_src`). Shipped in `9d6a240e` and
`af87e458`: product still in the first viewport, one filled control above the fold,
kickers only on Base camp / The summit, route draws with scroll, tally counts up once in
view, two photographs in `src/public/assets/hero/` under measured 0.9 wash bands (11.43:1
and 12.25:1). **`CREDITS.json` says "owner-supplied, licence to confirm" — fill in before
prod.** The stage half of that refresh (results regression: no question / no CORRECT word
at trivia RESULTS; the phase wipe; the performing reveal; arrivals on the wall) is
**mocked up, not built** — rationale §6 steps 3–5, open questions §7.

## 4. What to look at on test before approving prod

Org owner account: Plan & usage → Request the Team plan → strip states; Add questions on a
trivia set and on a Call & Answer set (auto-start both); Reports door on the welcome screen;
the front page at desktop and 390px.
Platform admin account: Plan requests (decide with a note), Organisations → Billing…
(grant a credit, see the preview, revoke), Discount codes (create, retire).
After 1 Oct: Billing history and an invoice.

## 5. Habits this session paid for

- A Python `open(p,'w')` chained with `open(p).read()` truncated two files; write from a
  variable, never `open(p,'w').write(open(p).read())`.
- Watch deploys on the code-split chunk, not `bundle.js`; minified variable names are not
  greppable — grep a class name or a string literal.
- The `hidden` attribute is defeated by `.form-group { display: flex }`; use `display: none`.
- Read the backend gate's output before pushing; one push went out with a flaky suite that
  turned out to be a real same-millisecond key collision (`0da1edac`).
